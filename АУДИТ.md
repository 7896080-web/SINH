# Аудит целостности и причинно-следственных связей (16.09.2026)

Полная проверка текущей версии: прогон тестов, круговой прогон миграций, сверка моделей
со схемой, сквозной прогон живого приложения, нагрузка, конкуренция двух процессов,
точечная безопасность и три независимых разбора кода (жизненный цикл заказа, канал 1С
и сверка, веб-слой).

**Каждая находка ниже воспроизведена на работающем коде.** Утверждения, полученные только
чтением исходников, помечены отдельно. Ничего не исправлялось: это отчёт, решение за
владельцем системы.

## Что заведомо в порядке

| Проверка | Результат |
|---|---|
| Тесты | 292 passed |
| Компиляция `app/`, `tests/`, `scripts/` | без ошибок |
| 16 шаблонов Jinja | разбираются, автоэкранирование включено, обхода `\|safe` нет |
| Миграции | 8 ревизий, одна голова, upgrade → downgrade base → upgrade проходит |
| Модели ↔ схема | `alembic autogenerate` даёт 0 расхождений |
| Ограничения БД | дубль заказа в кабинете, дубль настройки, баркод на два товара, задание без order_id — всё отбивается |
| Конкуренция двух процессов (SQLite + WAL) | 120 заказов и 120 правок одновременно: 0 ошибок блокировки, остаток сошёлся точно |
| Нагрузка страницы «Товары и остатки» | 1500 товаров × 5 кабинетов: страница 0.12 с, поиск 0.10 с, экспорт 0.11 с |
| Безопасность | пароли bcrypt, ключи площадок Fernet, сырого SQL нет, секретов в индексе нет |
| Сквозной прогон (33 шага) | 32 пройдено, 1 провален (находка 12) |

Держатся ключевые инварианты: списание остатка и постановка в очередь атомарны;
идемпотентность по `ProcessedOrder` на всех трёх путях; формат строк задания точно
соответствует разбору в живой обработке 1С; `.part`-файлы не читаются как готовые; BOM
на приёме обработан; опоздавший ответ 1С закрывает просроченное задание.

---

## КРИТИЧНО — приводит к оверселлу

> Все находки этого раздела исправлены: 1, 2, 3 — 16.09.2026 (pm20), 4 — 17.09.2026
> (pm24), 6 и 7 — 17.09.2026 (pm25), 5 — 17.09.2026 (pm26). Исходные сценарии больше не
> воспроизводятся. Описание оставлено как есть — это история дефекта и объяснение,
> почему код выглядит именно так.

### 1. ✅ ИСПРАВЛЕНО (pm20) — Сверка применяла снимок 1С часовой давности

`app/workers/scheduler.py` — запрос выгрузки и сама сверка стоят на одном часовом
интервале с разницей 10 секунд. Обработка 1С запускается по своему расписанию, поэтому
файл остатков появляется уже ПОСЛЕ прогона сверки и лежит до следующего — то есть
применяется примерно через час после снимка.

Воспроизведено:

```
12:20  1С сняла снимок: ЦС = 10, файл лёг в results
12:20–13:20  продано 2 шт, документы в 1С созданы, задания закрыты, «в пути» = 0
             у нас 8, в 1С 8
13:20  сверка читает старый файл: actual = 10, expected = 8 + 0 = 8, delta = +2
       остаток стал 10, на площадки поставлено 10 при реальных 8
```

Ошибка повторяется каждый час, в котором были продажи, и сама себя не лечит. В журнале
сверок сервера видна та же подпись: 17 → 38 → 29 с чередующимися дельтами +21 и −9 при
нулевом «в пути».

**Что делать:** сверка должна работать только по файлу, который заведомо новее последнего
запроса. Минимум — сравнивать время файла с временем отправки `EXPORT_STOCK_ON_HAND` и
пропускать устаревший, а не применять его.

### 2. ✅ ИСПРАВЛЕНО (pm20) — Товар, распроданный в 1С до нуля, не сверялся никогда

`ВыгрузитьОстаткиЦС` строит выборку по виртуальной таблице `ТоварыНаСкладах.Остатки`,
которая не возвращает позиции с нулевым остатком. `run_reconciliation` идёт только по
строкам файла, поэтому исчезнувший товар не сверяется вообще.

Воспроизведено: у товара в приложении 4 шт, в 1С физический ноль, строки в файле нет —
после сверки в приложении по-прежнему 4, и это число продолжает уходить на площадки.

**Что делать:** либо выгружать из 1С весь ассортимент с нулями, либо в сверке считать
отсутствующий в файле товар нулевым (осторожно: частичная выгрузка тогда обнулит всё).

### 3. ✅ ИСПРАВЛЕНО (pm20) — Синтетический тестовый заказ гасил боевой опрос кабинета WB

`poll_cancellations` отдаёт клиенту площадки идентификаторы всех необработанных заказов
кабинета, включая синтетические `TEST-…`. Клиент WB приводит их к целому числу.

Воспроизведено: `ValueError: invalid literal for int() with base 10: 'TEST-abc'`, падение
до сетевого запроса. Дальше исключение ловит планировщик, счётчик сбоев растёт, и на пятом
цикле предохранитель выставляет кабинету `is_active = False`. Кабинет перестаёт
опрашиваться и исключается из рассылки: заказы не принимаются, остатки не обновляются,
площадка продолжает продавать по последнему числу.

**Что делать:** исключить `TEST-` из выборки в `poll_cancellations` либо завести
`is_test` у `ProcessedOrder`.

### 4. ✅ ИСПРАВЛЕНО (pm24) — Сверка не вычитает «в пути» при ненулевой дельте

`app/workers/reconciliation.py`: `expected_1c` считается как остаток плюс «в пути», но
присваивается сырое `actual_1c` без вычитания «в пути».

Воспроизведено: было 10, заказ на 2 (задание не закрыто, «в пути» +2), в магазине продали
1 мимо площадок, 1С отдала 9. Остаток стал 9 вместо правильных 7, и 9 ушло на площадки.
Величина ошибки равна сумме незакрытых заданий по товару.

### 5. ✅ ИСПРАВЛЕНО (pm26) — Ошибка отправки на площадку терминальна, повтора нет

`app/workers/dispatch.py`: запись со статусом `error` нигде не возвращается в `pending`.

Воспроизведено: API вернул ошибку, запись помечена `error`, следующий цикл при живом API
не отправил ничего. Остаток списан у нас, площадка о нём не узнала до следующего события
по этому товару.

### 6. ✅ ИСПРАВЛЕНО (pm25) — Снятие галочки кабинета не отзывает остаток с площадки

`app/routers/products.py`, `toggle_sync`: при выключении только пишется журнал, в очередь
ничего не ставится. Одновременно гейт отбора начинает молча пропускать заказы этой пары.

Воспроизведено: после снятия галочки записей в очереди ноль, на площадке остаётся прежнее
число. Для сравнения, выключатель трансляции товара ставит запись и отправляет ноль.

Итог: площадка продолжает продавать остаток, о котором система больше не узнает ни одним
заказом и не создаст ни одного документа в 1С.

### 7. ✅ ИСПРАВЛЕНО (pm25) — Запись в очереди переживает снятие галочки кабинета

`app/transmit.py`, `quantity_for_account` не проверяет отметку кабинета и паузу площадки,
хотя `explain` для интерфейса их проверяет.

Воспроизведено: запись висела в очереди, галочку сняли, цикл рассылки отправил на
отключённый кабинет полный остаток.

---

## ВЫСОКО — потеря данных или неверные документы в 1С

> Весь раздел исправлен 17.09.2026: находки 8, 9, 10 — патч pm27, находки 11, 12, 13 —
> патч pm28. Находка 9 закрыта на стороне приложения полностью, но окончательная
> точность сопоставления требует одной строки в обработке 1С — см. `1c/README.md`.

### 8. ✅ ИСПРАВЛЕНО (pm27) — Ответ `ERROR` от 1С закрывает задание как успешное

Воспроизведено: `e1|ERROR|Не найден склад «…»` — задание переходит в `done`, аномалия не
заводится, на странице диагностики ничего нет. Перемещения в 1С не существует, а система
считает заказ проведённым.

### 9. ✅ ИСПРАВЛЕНО (pm27, частично) — Ответ 1С сопоставляется только по номеру заказа

`apply_result_batch` ищет задание по `order_id` без кабинета и без команды, беря самое
свежее. Воспроизведено: два кабинета с одинаковым номером заказа — один ответ закрыл
задание чужого кабинета, второе осталось висеть. Тот же механизм перекрещивает ответы
на `CREATE_MOVEMENT` и `CANCEL_MOVEMENT` одного заказа.

### 10. ✅ ИСПРАВЛЕНО (pm27) — Имя файла задания с точностью до секунды

Воспроизведено: два батча подряд получили одно имя `task_20260916220532.txt`. Публикация
делает замену, поэтому первый файл затирается, а его задания уже помечены отправленными и
в 1С не попадут никогда. Раз в сутки минутный `ftp_send` и суточный запрос справочника
приходятся на одну и ту же секунду.

### 11. ✅ ИСПРАВЛЕНО (pm28) — Отрицательный остаток из 1С клампится в ноль

`run_reconciliation` собирает максимум по баркодам с дефолтом 0, поэтому пересортица −3
превращается в 0 и в журнале сверки. Остальной код специально написан так, чтобы
отрицательный остаток сохранять.

### 12. ✅ ИСПРАВЛЕНО (pm28) — Симуляция на странице тестирования меняет то, что уходит в бой

Изоляция `is_test` защищает очередь рассылки и файл задания для 1С, но не общий
`stock_on_hand`, который читает боевая рассылка.

Воспроизведено: остаток 100, симуляция заказа на 7 → в базе 93; тестовая запись на
площадку не ушла (изоляция работает), но следующая боевая запись отправила 93 вместо 100.
Направление безопасное (недоотправка), самолечится ближайшей сверкой.

### 13. ✅ ИСПРАВЛЕНО (pm28) — Артикул и наименование из 1С не экранируют разделитель

В выгрузке остатков и справочника размер, цвет и баркод прогоняются через замену `|` на
пробел, а артикул и наименование — нет. Наименование вида «Джинсы|36» сдвигает поля:
количество читается из соседней колонки, баркод — из следующей. Проверено чтением обеих
сторон протокола, на живых данных не воспроизводилось.

---

## СРЕДНЕ — мониторинг, интерфейс, эксплуатация

> Находки 14, 15, 16 исправлены 17.09.2026 (патч pm29), 17 — 17.09.2026 (pm22),
> 19, 20, 21 — 17.09.2026 (pm30). Открыты 18, 22, 23.

### 14. ✅ ИСПРАВЛЕНО (pm29) — Мёртвый heartbeat отключённого кабинета держит `/health` в 503 навсегда

Воспроизведено: строка `poll_orders_account_7` осталась после снятия задания, `/health`
отдаёт 503 бессрочно. Удаления heartbeat нет нигде в проекте. Предохранитель отключает
кабинет штатно, то есть штатное срабатывание защиты навсегда красит мониторинг.

### 15. ✅ ИСПРАВЛЕНО (pm20 + pm29) — `/health` не видит, что часовой запрос выгрузки перестал работать

Все три задания на отправку в 1С пишут heartbeat под одним именем `ftp_send`, который
обновляет минутное задание. Если часовой запрос выгрузки остановится, мониторинг
останется зелёным, а сверка встанет.

### 16. ✅ ИСПРАВЛЕНО (pm29) — `/health` отдаёт текст ошибки воркера анонимно

Воспроизведено: анонимный запрос видит `last_error` с адресом эндпоинта площадки.
Докстринг обещает, что чувствительного там нет.

### 17. ✅ ИСПРАВЛЕНО (pm22) — Ограничение 300 строк применялось до фильтров

Воспроизведено: 351 товар с нулевой отправкой, фильтр «только те, где уходит 0» показал
300; экспорт при 400 товарах отдал 300 строк без предупреждения. Оператор считает, что
выгрузил всё.

### 18. Сообщение об ошибке ввода не доходит до оператора

Воспроизведено: ввод «12,5» в порог трансляции — строка перерисовывается молча, текста
ошибки нет; плашка всплывает позже на полной странице без контекста. Фрагменты HTMX не
наследуют базовый шаблон, а флеш снимается только на полной странице.

### 19. ✅ ИСПРАВЛЕНО (pm30) — Битый файл Excel роняет импорт в 500

Воспроизведено: `BadZipFile` наружу. Все три импорта аккуратно собирают ошибки по строкам,
но падают на открытии файла.

### 20. ✅ ИСПРАВЛЕНО (pm30) — «Загрузить заказы с площадки» молча пишет в базу

Кнопка и докстринг обещают просмотр без изменений. Воспроизведено: дата старта задним
числом у товара изменилась с пустой на 2026-08-07 без сообщения и без записи в аудит.

### 21. ✅ ИСПРАВЛЕНО (pm30) — Бэкфилл не откатывает сессию при ошибке

В обработчике исключения нет `db.rollback()`, поэтому после сбоя коммита следующий же
запрос к базе выбросит ошибку наружу: часть заказов проведена, оператор видит 500 и,
скорее всего, повторит прогон.

### 22. Ручной остаток дрейфует вверх

Воспроизведено: было 2, заказ на 3 дал 0 (потеряна единица), отмена вернула 3. Приём и
отмена не обратны из-за ограничения снизу нулём. Поле устаревшее, но на товарах с прошлых
версий оно есть.

### 23. Пробелы в журнале действий

Смена и стирание токена площадки, смена склада кабинета и массовый импорт баркодов не
пишутся в журнал аудита, хотя соседние операции пишутся. Проверено чтением.

---

## Мелкое

- `products_rows.html`: `colspan` считается как 10 плюс кабинеты, колонок 11.
- `app/transmit.py`: в ветке порога игнорируется переданный остаток из очереди, docstring
  обещает обратное. Практически безвредно, но docstring врёт.
- `Transmit.ok` и передача `account_label` в шаблон не используются нигде.
- `scripts/patch_sizecolor.py`, `pmA.py`, `pmB.py`, `patch_worker_seed.py` — разовые
  патчи под файлы, которых больше нет; запуск перезапишет актуальный код устаревшим.
- `CLAUDE.md` утверждает, что `scripts/smoke_test.py` умеет симулировать заказ через HTTP.
  Он делает только GET-запросы. Там же требование «187 тестов» при фактических 292.
- `scripts/smoke_test.py` проверяет `/sync-products` вместо `/products`.
- Документация протокола в `1c/README.md` и `1c/ТЗ_ОбменССайтом.md` отстала: описаны 7
  полей вместо 8, нет команды `EXPORT_BARCODES`, нет размера и цвета в строке остатков.
- `README.md` и `АРХИТЕКТУРА.md` ссылаются на удалённые модули страниц.

## Не проверено на живых данных

Разбор кода обработки 1С указывает ещё на три риска, которые нельзя воспроизвести без
рабочей базы: отмена перемещений не идемпотентна (обратный документ сам подходит под
шаблон поиска, повторная отмена создаёт фантомный приход); файл задания при сбое
публикации результата обрабатывается повторно целиком; метка времени результата тоже
посекундная. Их стоит проверить на тестовой базе 1С.


---

## Исправлено 16.09.2026 (pm20)

**1. Свежесть снимка.** `job_ftp_send` теперь пишет отдельный heartbeat для каждого из
трёх расписаний, и время часового запроса выгрузки служит отметкой «когда просили».
`fetch_stock_export_rows` применяет только снимок новее этой отметки, устаревшие
архивирует без разбора. Из нескольких свежих берётся самый новый: склеивать два снимка
нельзя, иначе распроданный товар воскресает из старого файла. Сверка сдвинута на 5 минут
после запроса, чтобы 1С успела ответить. Заодно закрыта слепота `/health`: остановка
часового запроса выгрузки, то есть фактическая остановка сверки, теперь видна.

**2. Исчезнувший товар.** `run_reconciliation(..., missing_means_zero=True)`: товар с
баркодами и ненулевым остатком, которого нет в полном снимке, получает ноль и проходит
обычный путь сверки — журнал, рассылка, учёт «в пути». Защита от обрезанного файла: если
снимок покрывает меньше половины прежних ненулевых товаров, обнуление не применяется, а в
статистике появляется `snapshot_suspicious`.

**3. Синтетика в API площадки.** `poll_cancellations` и `poll_confirmations` берут заказы
через `_real_open_orders`, который исключает префикс `TEST-`. Префикс объявлен один раз в
`order_poller.py`, страница тестирования импортирует его оттуда.

Проверено: 304 теста, три исходных сценария воспроизведены до правки и не воспроизводятся
после. Сквозной прогон `scripts/e2e_audit.py` — 32 из 33 (шаг 27 — открытая находка 12).


## Исправлено 17.09.2026 (pm22)

**Находка 17 — фильтры не работали на большом каталоге.** Отбор ушёл в SQL и применяется
ДО ограничения по количеству строк. Проверено на каталоге в 152 000 SKU, где предложения
есть только у последних 119 товаров по алфавиту: «Только с предложениями» находит все 119
за 0.4 с, раньше показывал пусто. Страница честно пишет «Показано 300 из N», а обрезанная
выгрузка в Excel помечается последней строкой прямо в файле — иначе оператор правит её и
импортирует обратно, считая, что охватил весь каталог.

**Новый фильтр «Скрыть размер U».** Прячет позиции с универсальным размером; регистр и
пробелы не важны, товары без размера остаются. Действует и на странице, и в выгрузке.

**Мелочи оттуда же:** `colspan` пустой строки считался на колонку меньше; страница
«Мэппинг» падала в 500, если у записи не проставлена дата создания.


## Исправлено 17.09.2026 (pm24)

**Находка 4 — сверка возвращала на склад уже отгруженные единицы.** Учёт устроен так:
заказ с площадки уходит в перемещение ЦС → склад площадки сразу, подтверждения с нашей
стороны не ждём; остаток списывается у нас в момент приёма заказа, а документ в 1С
появляется только после ответа по заданию. Пока задание не закрыто, 1С показывает
единицы, которых у нас уже нет. Сверка это знала (`expected_1c = остаток + «в пути»`), но
при ненулевой дельте присваивала сырое `actual_1c` — и «в пути» возвращалось на склад и
уезжало на площадки второй раз. Теперь применяется `actual_1c − «в пути»`, что тождественно
равно `остаток + дельта склада`: остаток двигается ровно на складское движение, как и
`transmit_override` рядом. Возврат по нашей отмене (`CANCEL_MOVEMENT`) работает
симметрично — «в пути» там отрицательный.

**Там же: «в пути» считается на момент снимка, а не на момент сверки.** Между выгрузкой
1С и запуском сверки штатно проходит около 5 минут, и задание, закрытое в этом промежутке,
в самом снимке ещё не проведено. Считать его закрытым значило бы принять уже отгруженные
единицы за приход на склад — то есть та же ошибка с другого конца. `run_reconciliation`
получила параметр `snapshot_at`, а `ftp_channel` — `fetch_stock_export_snapshot`, которая
отдаёт строки вместе со временем файла. Когда время неизвестно (разовый скрипт, ФС не
отдала mtime), поведение прежнее — считаем только по открытым сейчас заданиям.

**Отрицательный остаток после сверки сохраняется.** Если магазин продал глубже, чем мы
успели отгрузить, честный остаток отрицательный; на площадки при этом уходит 0. Прятать
это нулём нельзя — иначе пересортица не видна ни в журнале, ни оператору. (Отдельная
находка 11 — про клампинг отрицательных чисел на входе сверки — остаётся открытой.)

Проверено: 326 тестов зелёные; 12 новых в `tests/test_reconciliation_in_flight.py`, из них
7 падают на прежнем коде (проверено подстановкой старой строки). Граница `is_test`
сохранена: симулированные задания не попадают в «в пути» ни открытыми, ни закрытыми
после снимка.


## Исправлено 17.09.2026 (pm25)

**Находки 6 и 7 — одна и та же трещина.** Интерфейс (`transmit.explain`) проверял отметку
кабинета и паузу площадки, а рассылка (`transmit.quantity_for_account`) — нет: считалось,
что очередь копится только по отмеченным кабинетам и дублировать проверку незачем. Из
этого расхождения выходили сразу два пути оверселла.

**7 — запись переживала снятие галочки.** Элемент очереди, созданный при отмеченном
кабинете, оставался в ней после снятия отметки, и цикл рассылки отправлял на отключённый
кабинет полный остаток. Теперь `quantity_for_account` проходит всю лестницу целиком:
выключатель товара, отметка кабинета, пауза площадки, пороги. Интерфейс и рассылка считают
одним кодом — это проверяется таблицей из шести состояний, где число в строке товара и
число, уходящее на площадку, сравниваются напрямую.

**6 — снятие галочки не отзывало остаток.** Раньше писался только журнал: на площадке
оставалось последнее отправленное число, она продолжала продавать, а заказы по снятой паре
гейт отбора уже пропускал — ни списания у нас, ни документа в 1С. Теперь снятие ставит в
очередь ноль (`transmit.enqueue_withdrawal`, причина `manual_disable`) — ровно так же, как
всегда вёл себя главный выключатель товара. В журнал пишется «(в очередь 0)».

**Граница, которую специально не трогали:** пауза рассылки на кабинет — это «подождать», а
не «отозвать». Очередь копится и уйдёт при снятии паузы, ноль сама по себе пауза не
отправляет. Это закреплено тестом, чтобы не «исправить» заодно и её.

**Тесты пришлось поправить в четырёх местах** (`test_broadcast_offset.py`, `test_dispatch.py`,
`test_web_testing.py`): они проверяли арифметику порогов и резерва, не заводя `SyncSetting`
вовсе, то есть опирались ровно на дефект находки 7. В бою неотмеченных пар в очереди не
бывает — все места постановки в очередь отбирают только `SyncSetting.enabled`; это
проверено по всем шести местам создания `DispatchQueueItem`.

Проверено: 341 тест зелёный; 15 новых в `tests/test_dispatch_gate.py`, из них 8 падают на
прежнем коде (проверено откатом обеих правок).


## Исправлено 17.09.2026 (pm26)

**Находка 5 — первая же ошибка площадки выводила запись из работы навсегда.** Статус
`error` не возвращался в `pending` нигде в проекте: API ответил 503 один раз — и
следующий цикл при уже живом API не отправлял ничего. Остаток списан у нас, площадка о
нём не узнала до следующего события по товару, то есть продолжала продавать то, чего нет.

Теперь сбой не терминален: запись остаётся `pending`, счётчик попыток растёт, и следующая
попытка откладывается на 1, 2, 5, 15 минут (`MAX_ATTEMPTS = 5`, всего около 23 минут
покрытия). `error` ставится, только когда попытки исчерпаны, и такая запись из работы
выходит — иначе цикл вечно возил бы заведомо неотправимое значение. Постоянные причины
(нет баркода) остаются терминальными сразу: повтором они не лечатся.

**Повтор не воскрешает устаревшее число.** Это главный риск самой идеи повторов, поэтому
схлопывание по товару теперь смотрит на ВСЕ ожидающие записи, включая отложенные после
сбоя: пришло новое значение — отложенное поглощается им и на площадку не уйдёт. И
наоборот: если самая свежая запись на паузе, более старую вместо неё не отправляем.

**Схема:** `dispatch_queue.next_attempt_at` (nullable DateTime), миграция
`e5b1c7a94d20`. NULL означает «можно прямо сейчас», поэтому существующие записи очереди
после апгрейда ведут себя ровно как раньше. Апгрейд и откат проверены на SQLite.

**Видно оператору:** на странице «Диагностика» у кабинета появился счётчик «повтор: N»
(записи, которые сорвались и ждут следующей попытки) рядом с прежним «ошибок: N»
(попытки исчерпаны). Раньше сорвавшаяся запись сразу падала в «ошибок», и отличить
«площадка моргнула» от «так и не ушло» было нельзя.

Проверено: 352 теста зелёные; 11 новых в `tests/test_dispatch_retry.py`, из них 8 падают
на прежнем коде.

**Заодно из раздела «Мелкое»:** `scripts/smoke_test.py` проверял удалённую страницу
`/sync-products` (то есть заведомо падал) — теперь `/products`; в `CLAUDE.md` поправлено
число тестов и убрано обещание, будто smoke_test умеет симулировать заказ по HTTP.


## Исправлено 17.09.2026 (pm27)

**Находка 8 — `ERROR` больше не выглядит успехом.** Появился статус задания `failed`
(«1С ответила отказом, документа НЕТ»), отдельный от `done`. У него есть последствия,
ради которых всё и делалось: такое задание продолжает считаться «в пути» при сверке.
1С документа не создала — значит у себя она товар не списала, и если перестать вычитать
эти единицы, сверка вернёт уже проданное на склад и отправит его на площадки второй раз.
Заодно по той же логике «в пути» теперь считаются и просроченные (`timeout`) задания:
создан документ или нет — неизвестно, а ошибиться тут можно только в сторону
недоотправки. На «Диагностике» у кабинета появился счётчик «отказов 1С».

**Находка 9 — ответ больше не закрывает чужое задание.** Раньше задание искалось по
одному номеру заказа среди всех кабинетов и команд и бралось САМОЕ СВЕЖЕЕ. Теперь берётся
самое СТАРОЕ незакрытое (1С отвечает в том же порядке, в каком получила строки), и в
пределах одного файла одно задание закрывается только один раз — два ответа закроют два
задания, а не одно дважды.

Полную точность это не даёт: в ответе 1С нет ни кабинета, ни команды, поэтому разобрать
создание и отмену одного заказа приложение может только по порядку. Разбор теперь
ПРИНИМАЕТ необязательное последнее поле с именем команды (`o1|OK|ЦБ-1|CREATE_MOVEMENT`)
и при его наличии требует совпадения. Это две строки в обработке 1С; пока их нет,
работает прежний, менее точный порядок — формат обратно совместим в обе стороны.

**Находка 10 — имя файла задания.** В имени теперь микросекунды, а публикация
ОТКАЗЫВАЕТСЯ перезаписать существующий файл (проверяется и каталог заданий, и архив):
затирание означало бы, что строки, уже помеченные отправленными, в 1С не попадут никогда
и молча. Имя подбирается ДО пометки строк отправленными — занятое имя не может стоить
целого батча.

**Осторожно, обратная сторона:** раньше столкновение имён гасило один из двух файлов у
нас. Теперь до 1С доходят оба — и если она обработает их в одну и ту же секунду, уже её
собственная метка (`Метка = Формат(ТекущаяДата(), …ЧЧммсс)`) даст одинаковые имена
`result_*.txt`, и один результат затрёт другой. Задания из затёртого уйдут в `timeout`
(то есть останутся «в пути» — направление безопасное), но разбирать их придётся руками.
Однострочное исправление на стороне 1С описано в `1c/README.md`.

Проверено: 371 тест зелёный; 19 новых в `tests/test_1c_channel_results.py`, из них 8
падают на прежнем коде.


## Исправлено 17.09.2026 (pm28)

**Находка 11 — минус больше не превращается в ноль.** Максимум по баркодам одного товара
начинался с нуля, поэтому пересортица −3 приходила в остаток и в журнал сверки как 0.
Теперь первое значение берётся как есть, максимум между баркодами сохранён (это один
физический остаток, а не сумма). На площадку по-прежнему уходит `max(0, …)` — клампит
рассылка, а не учёт.

**Находка 12 — симуляция больше не управляет боем.** Раньше тестовый заказ списывал
боевой `stock_on_hand` (и ручной `transmit_override`), и следующая РЕАЛЬНАЯ рассылка
отправляла на площадку заниженное тестом число: остаток 100, симуляция на 7 → уходило 93.
Теперь `process_new_order`/`process_cancellation` при `is_test=True` боевые поля не
трогают вовсе, а правдоподобное число считают отдельно: боевой остаток минус открытые
тестовые заказы. Цепочка симуляций по-прежнему выглядит как настоящая
последовательность (100 → 93 → 90), тестовые записи очереди несут именно её, а в карточке
товара рядом с боевым остатком появилась пометка «в симуляции: N».

Важное следствие: **очистка тестовых данных больше НЕ возвращает остаток.** Раньше тест
списывал, а очистка возвращала; если бы возврат остался при неснимающей симуляции, каждая
очистка молча ДОБАВЛЯЛА бы товар на склад. Это закреплено отдельным тестом.

**Находка 13 — разделитель внутри наименования.** Обработка 1С чистит `|` в размере,
цвете и баркодах, но не в артикуле и наименовании: «Джинсы|36» сдвигало поля, количество
читалось из соседней колонки, а баркод — из следующей. Хвост строки (количество, баркоды,
размер, цвет) имеет фиксированную длину, поэтому разбор теперь режет строку СПРАВА, а всё
лишнее отдаёт наименованию — оно и так текст. Критичные поля (uid, количество, баркоды)
не сдвигаются, даже если `|` попадёт и в артикул. Старый формат из пяти полей
разбирается как раньше. Экранирование на стороне 1С всё равно желательно — строка
описана в `1c/README.md`.

Проверено: 389 тестов зелёные; 16 новых в `tests/test_audit_11_12_13.py`, из них 8 падают
на прежнем коде. Три теста страницы «Тестирование» переписаны: они проверяли, что
симуляция списывает боевой остаток, — то есть закрепляли сам дефект.


## Исправлено 17.09.2026 (pm29)

**Находка 14 — отключённый кабинет больше не держит мониторинг красным.** Раньше строка
`poll_orders_account_7` оставалась навсегда после снятия задания, протухала и держала
`/health` в 503 бессрочно; предохранитель отключает кабинет ШТАТНО, то есть штатное
срабатывание защиты навсегда красило мониторинг — а мониторинг, который всегда красный,
никто не читает. Теперь чинится с двух сторон: `reconcile_account_jobs` удаляет heartbeat
вместе с заданием (в статистике появилось `heartbeats_dropped`), а `/health` вдобавок
пропускает heartbeat-ы кабинетов, которых больше нет или которые отключены — это
покрывает и строки, оставшиеся от прошлых версий. Число таких строк отдаётся как
`ignored_workers`, число отключённых кабинетов — как `disabled_accounts`: прятать их
нельзя, но и в 503 они больше не превращаются. Опрос ЖИВОГО кабинета проверяется как
раньше — на это есть отдельный тест, чтобы «исправление» не свелось к отключению
мониторинга.

**Находка 15 — пропавший воркер теперь виден.** Разные имена heartbeat для трёх
расписаний отправки сделаны ещё в pm20, но оставалась дыра посерьёзнее: `/health` проверял
только те строки, что уже есть в базе. Если задание не навесилось вовсе (ошибка в
расписании, потерянный job), строки просто не было — и проверять было нечего, мониторинг
оставался зелёным, хотя часовой запрос выгрузки не делался и сверка стояла. Теперь есть
список обязательных воркеров (`REQUIRED_WORKERS`) и `missing_workers` в ответе.

Чтобы это не давало ложную тревогу после каждого деплоя, планировщик при старте пишет
отметку `scheduler_start`, и пропажа судится только по времени работы: воркеру даётся срок
на первый прогон. Пока отметки нет вовсе (старая база сразу после обновления) пропажу не
судим совсем. Сама отметка в список воркеров не попадает и на протухание не проверяется —
иначе она, никогда не обновляясь, покрасила бы мониторинг сама.

**Находка 16 — текст ошибки наружу не уходит.** `/health` открыт без авторизации, а
`last_error` содержал, например, адрес эндпоинта площадки. Теперь наружу отдаётся факт
ошибки (`last_success: false` и короткая заглушка вместо текста), а сам текст — на
странице «Диагностика», под логином. Имён кабинетов в ответе тоже нет: только счётчики.

Проверено: 402 теста зелёные; 13 новых в `tests/test_health_monitoring.py`, из них 5
падают на прежнем коде. Два прежних теста `/health` заводили heartbeat кабинета, которого
нет в базе, — им добавлен реальный активный кабинет, иначе они проверяли бы не то.


## Исправлено 17.09.2026 (pm30)

**Находка 21 — бэкфилл больше не роняет прогон целиком.** После неудачного коммита внутри
`process_new_order` сессия остаётся сломанной, и СЛЕДУЮЩЕЕ же обращение к базе — хоть
запись в журнал — вылетает наружу. Оператор видел 500 на частично проведённом прогоне и,
скорее всего, повторял его. Теперь в обработчике исключения сначала `db.rollback()`, потом
запись об ошибке, и прогон продолжается с остальными заказами. Строка журнала об успешном
заказе коммитится сразу, чтобы не пропасть при откате на следующем. Та же однострочная
беда была в отправке остатка со страницы тестирования (`push_stock`): там после `except`
шёл `db.commit()` на сломанной сессии — тоже 500 вместо понятного сообщения; исправлено
заодно.

Воспроизведение в тесте — настоящее нарушение уникальности (кабинет + номер заказа) с
последующим коммитом, а не искусственный `rollback()`: иначе тест проверял бы не то.

**Находка 20 — просмотр заказов больше ничего не пишет.** Кнопка и докстринг обещают
просмотр без изменений, а `_resolve_start` молча сохраняла дату старта задним числом.
Теперь у неё есть флаг `persist`, по умолчанию выключенный: просмотр читает (введённая
дата побеждает, пустое поле — берём сохранённую), но не пишет. Сохраняет только бэкфилл —
явное действие оператора, — и делает это с записью в журнал действий
(`active_since_changed`), чтобы изменение перестало быть молчаливым. Отдельная дата на
странице «Товары и остатки» работает как работала.

**Находка 19 — битый файл Excel больше не роняет импорт в 500.** Все три импорта
(`/products/import`, `/mapping/import`, `/anomalies/import`) аккуратно собирают ошибки по
строкам, но падали на ОТКРЫТИИ файла, ещё до первой строки: наружу летел `BadZipFile`.
`read_xlsx_rows` теперь бросает `ExcelReadError` с человеческой причиной (не тот формат,
обрезанная загрузка, пустой файл), а все три роутера показывают её обычным сообщением и
возвращают оператора на страницу.

Проверено: 416 тестов зелёные; 14 новых в `tests/test_audit_19_20_21.py`, из них 10 падают
на прежнем коде.
