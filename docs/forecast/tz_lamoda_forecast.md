# ТЗ: система прогнозирования спроса и пополнения — Lamoda FBO, WB/Ozon (FBS), собственный сайт

## 1. Назначение

Система решает три задачи:
1. Прогноз спроса по SKU (артикул-цвет-размер) — многоканально: Lamoda (FBO), WB и Ozon (FBS), собственный сайт (раздел 5)
2. Расчёт рекомендаций по пополнению склада Lamoda (FBO — единственный канал с отдельным складом площадки, раздел 5.5/6)
3. Рекомендации по закупке у поставщика (по агрегированному спросу со всех каналов сразу, раздел 5.5) и по ценообразованию с учётом маржинальности (тоже многоканально для Lamoda/WB/Ozon, раздел 5.7)

Работаем по одному ИП. Сопоставление между собственным каталогом и всеми площадками — по пулу баркодов цвето-размерного SKU (тот же принцип, что уже используется в синхронизации остатков с WB/Ozon/Kit).

---

## 2. Архитектура (потоки данных)

⚠️ **Критично для реализации: везде далее в ТЗ методы Lamoda API названы по v1** (`v1.nomenclature.update-price`, `v1.nomenclatures.list`, `v1.attributes.sku.list` и т.п.) — так была устроена доступная на момент написания документация. **Оба API v1 (Lamoda Seller Partner API v1 JSON-RPC и Lamoda B2B Platform Partner API v1 REST) официально отключаются 01.01.27** (по отдельной странице документации "Версии API и совместимость" — более раннее упоминание 30.11.2026 в одном из источников, видимо, относилось к части методов, не всему API целиком; ориентироваться на 01.01.27 как на подтверждённую дату полного отключения). Официальная позиция Lamoda: если интеграции ещё нет — делайте её сразу на v2 (`Lamoda Seller Partner API v2`, REST, единый для FBS и FBO, базовый URL `https://public-api-seller.lamoda.ru/api`). **Таблица соответствия методов v1→v2, использованных в этом ТЗ — раздел 7.** Перед реализацией сверить со справочником соответствия методов Lamoda (раздел «Миграция с v1 на v2» документации) — названия/структура параметров v2 отличаются (camelCase вместо snake_case, суммы объектами `{amount, currency}` в минорных единицах).

```
Входные данные:
  1С (остатки своего склада — ЦС, через FTP-мост)
  Lamoda API v2 (остатки FBO, продажи, возвраты, акции, категории — раздел 4)
  WB Statistics API + Ozon FinanceAPI (текущие продажи — постоянный поток, раздел 4)
  Собственный сайт awer-russia.ru / Яндекс KIT (заказы — CSV/XLSX, раздел 4/9)
  Историческая выгрузка WB/Ozon/розница (5-7 лет — разовая загрузка, сезонность + новинки, раздел 4)
  Размерный профиль — собственный расчёт из истории заказов (не из Lamoda API)
  Вордстат + Google Trends + клики Директа + Метрика (коэф_интерес, раздел 5.3)
  Конкурентная аналитика по категориям (при платной подписке)
  Погода (Open-Meteo)
  ЦБ РФ (курс USD)
        │
        ▼
  Модуль прогноза спроса по SKU — многоканальный (Lamoda/WB/Ozon/собственный сайт, раздел 5)
        │
        ├──► Точка заказа и страховой запас для FBO Lamoda (только Lamoda, раздел 5.5)
        │         │
        │         ▼
        │    Отгрузка на FBO Lamoda ──► УПД / Честный Знак / ЭТрН (существующий пайплайн)
        │
        └──► Агрегированный спрос на ЦС по всем каналам (Lamoda+WB+Ozon+сайт+розница, раздел 5.5)
                  │
                  ▼
             Закупка у поставщика (если своего остатка не хватает,
             и товар отмечен как доступный к заказу — поле ведётся вручную)
        │
        ▼
  Модуль маржинальности и ценообразования — многоканальный для Lamoda/WB/Ozon (раздел 5.7),
  включая оптимизацию по линейке (раздел 5.11, пока только Lamoda)
```

---

## 3. Справочники (master data)

### Товар (SKU)
```
sku_id, артикул, категория, подкатегория, сезон (лето/зима/демисезон),
поставщик_id, lead_time_производства_дни, lead_time_доставки_от_поставщика_дни,
дата_первой_продажи, статус (новинка / активный / затухающий / снят),
доступен_к_заказу_у_поставщика (bool, ведётся вручную),
дата_снятия_с_производства (null если доступен),
тип_коллекции (базовая / капсульная — капсульная не идёт в регулярный цикл пополнения),
теги_фасона (если ведутся — для более точного подбора аналогов новинкам),
ставка_НДС,
плановая_цена_запуска (источник Cu для newsvendor-расчёта новинок, раздел 5.6, пока нет реальных продаж),
seasonal_group (группа по паттерну сезонности, не по категории — раздел 5.2),
color_group (для норматива распределения по цвету — раздел 5.6)
```

### Размерная сетка
Отдельная строка на каждый SKU + размер — прогноз и остатки считаются на этом уровне.

### Поставщик
```
поставщик_id, название, срок_производства_дни, срок_доставки_дни, мин_партия (MOQ)
```

### Сопоставление SKU ↔ Lamoda
```
артикул, цвет, размер, баркод, seller_sku, lamoda_sku
```
Источник (v2): `GET /v2/nomenclatures` — `sku`/`sellerSku`/атрибуты цвета и размера (v1-эквивалент `v1.nomenclatures.list` — не использовать, раздел 7).

⚠️ **Начальная загрузка — из уже существующего Excel-файла сопоставления, не с нуля через API.** Готовая карта уже ведётся (переиспользуем из проекта «Синхронизация Остатков» или отдельный файл, если он там не совпадает) — импортируется один раз при разворачивании системы. **Ключ сопоставления — баркод цветоразмерного SKU** (`product_size`, не артикул целиком) — через `barcode_pool` (раздел 10): один баркод однозначно указывает на конкретные артикул+цвет+размер, тогда как артикул сам по себе неоднозначен между площадками (у WB/Ozon/Lamoda разные внутренние SKU на один и тот же физический товар, но баркод общий). API (`GET /v2/nomenclatures` выше) используется дальше для новых позиций и сверки, не для построения сопоставления заново. Тот же файл и тот же ключ (баркод) — основа для сопоставления исторической выгрузки с других каналов (раздел 4, `external_channel_sales_history`).

⚠️ **У одного цветоразмерного SKU может быть несколько баркодов одновременно** — не только исторически (переиздание баркода, что уже покрывает `valid_from`/`valid_to` в `barcode_pool`), но и параллельно: разные баркоды на одной площадке и в 1С для одного и того же физического товара. Логика сопоставления это учитывает — **совпадение любого одного баркода из пула `barcode_pool` для данного `product_size` достаточно** для связывания записи, не требуется совпадение по единственному "каноническому" баркоду. Join идёт через весь пул, не через одно поле-баркод на `product_size`.

### Справочник аналогов (для новинок)
```
sku_id_новинка, sku_id_аналог, вес_сходства (0-1)
```
Критерии сходства: категория/подкатегория, сезон, ценовой сегмент, цветовая группа, теги фасона (если есть), давность запуска аналога (не старше 2-3 сезонов).

### Настройки маржи (редактируемые, не хардкод)
```
ставка_комиссии_lamoda = 42%
налоговый_режим = УСН доходы
ставка_НДС = 5%
ставка_УСН = 6%
мин_маржа = 30%, целевая_маржа = 50%, макс_маржа = 80%
корректировка_курса = +4 (руб., прибавляется к курсу ЦБ; может быть отрицательной)
курс_USD_порог (задать индивидуально по товару через себестоимость и текущую маржу)
liquidation_recovery_rate = 0.5 (доля себестоимости, возвращаемая уценкой неликвида — используется в newsvendor-расчёте объёма первой поставки, раздел 5.6)
max_step_pct (guardrail на максимальный шаг изменения цены за раз — разделы 5.7/5.11/6, значение задать индивидуально при внедрении)
outlier_sigma_threshold = 3.0 (фильтр выбросов при расчёте σ, раздел 5.5)
```

### Справочник промо-дат Lamoda
```
дата_начала, дата_окончания, название_акции (известные календарные акции — чёрная пятница и т.п.,
заносится заранее вручную)
```

---

## 4. Входные потоки данных

⚠️ **Историческая выгрузка с других каналов (WB, Ozon, 2 розничных магазина) — отдельная категория, не ежедневный поток.** Lamoda и собственный сайт дают всего несколько месяцев истории; WB/Ozon/розница — 5-7 лет. Это разовая (или редкая, при необходимости) загрузка исторических данных — назначение здесь: не текущие остатки/продажи, а сезонный индекс и история для новинок (раздел 5.2/5.6).

⚠️ **Отдельно от исторической загрузки — постоянный поток по WB и Ozon для прогноза спроса и маржинальности на текущий момент (раздел 5.2-5.7-доп).** В отличие от истории выше, это уже ежедневный/еженедельный автопайплайн, не разовая загрузка: текущие продажи (те же API, что и для истории — Statistics API WB / FinanceAPI Ozon) + тарифы/комиссии по категориям (WB Tariffs API; Ozon — точный метод уточнить, раздел 9). Розница в этот постоянный поток не входит — только площадки с понятной комиссионной структурой и API-доступом к продажам.

| Канал | Источник | Формат | Назначение |
|---|---|---|---|
| Wildberries | Statistics API — `GET /api/v5/supplier/reportDetailByPeriod` («Отчёт о продажах по реализации», `statistics-api.wildberries.ru`) — отдельный токен (Statistics API Key, не тот же, что для заказов/контента); лимит 1 запрос/мин (всплеск до 10) — закладывать паузы. Готовый клиент — `wbsdk` (Python) | по баркоду, помесячно за весь период | сезонный индекс, история для новинок |
| Ozon | Seller API — `AnalyticsAPI`/`FinanceAPI`, отчёты о продажах товаров (`docs.ozon.ru/api/seller`). Авторизация — заголовки `Client-Id`+`Api-Key`, не Bearer-токен | по баркоду, помесячно | то же |
| Розница (2 магазина) | 1С (если продажи розницы уже заведены в 1С — уточнить; если нет — отдельная выгрузка кассовой системы) | по баркоду цветоразмера, помесячно | то же |
| Собственный сайт (awer-russia.ru, на платформе Яндекс KIT) | Публичного API для заказов у Яндекс KIT не найдено (свежая платформа, запущена в сентябре 2025) — только выгрузка заказов CSV/XLSX из панели управления (раздел «Заказы») и Telegram-бот с уведомлениями о новых заказах (не полноценный поток данных, просто пинг) | выгрузка CSV/XLSX вручную/по расписанию, по баркоду | канал спроса, тянущий с ЦС напрямую (DBS-подобная модель — свой сайт, своя доставка) |

⚠️ **WB и Ozon — модель FBS, не FBO.** В отличие от Lamoda, у этих площадок нет собственного промежуточного склада — отгрузка идёт прямо с ЦС по каждому заказу. Это значит: страница «Поставка на FBO» (раздел 6) относится только к Lamoda — для FBS-каналов нет решения «сколько отгрузить на склад площадки», это не применимо по определению модели. Собственный сайт (awer-russia.ru) — та же логика, своя доставка напрямую с ЦС, только без маркетплейса вообще. Спрос со всех этих каналов так же тянет остаток с ЦС, просто напрямую — см. ниже про агрегированный спрос для раздела 5.5/6.

⚠️ **Известное расхождение данных WB (подтверждено сообществом продавцов, не наша догадка):** `pricewithdisc` не совпадает между методом "Заказы" и методом/отчётом "Продажи" — для расчётов истории ориентироваться на отчёт о продажах по реализации, не на заказы.

Ключевое условие использования — сопоставление по **баркоду цветоразмерного SKU** (`barcode_pool`, раздел 3/10), не по артикулу целиком и не по внутреннему SKU каждой площадки (они разные) — тот же ключ, что уже используется для начальной загрузки карты сопоставления с Lamoda (раздел 3). Ozon Seller API подтверждает баркод как рабочий ключ и на своей стороне — прямой метод поиска отправления по баркоду (`POST /v2/posting/fbs/get-by-barcode`).

| Поток | Источник | Периодичность | Ключевые поля |
|---|---|---|---|
| Остатки своего склада (ЦС) | 1С через FTP-мост (переиспользуем существующую синхронизацию) | по текущему циклу синхронизации | sku_id, размер, кол-во, дата_среза |
| Остатки FBO + stockout-флаг | `GET /v2/fbo/stocks` (фильтр `withZeroQuantity`) | ежедневно | sku_id, размер, склад, кол-во, дата_среза |
| Неликвиды | `GET /v2/stocks/illiquid` | еженедельно | sku_id, признак неликвида |
| Продажи | `GET /v2/orders`, агрегация `items[].status = Delivered` | ежедневно | sku_id, размер, дата, кол-во, цена_факт, цена_база |
| Возвраты | `/v2/orders`, `items[].status` ∈ {возвратные статусы} — единственный автоматизируемый источник (было резервным, отчёт «Воронка» без API исключён) | ежедневно | sku_id, размер, дата_доставки, дата_возврата |
| Размерный профиль | Собственный расчёт — агрегация исторических продаж по размерам из `/v2/orders` (не отчёт Lamoda, у него нет API) | ежедневно (накопительно) | категория, размер, доля_продаж |
| Категории FBO | `GET /v2/fbo/categories` | разово + при изменениях | дерево категорий |
| Конкурентная аналитика по категориям | Внешний сервис аналитики маркетплейсов (MPSTATS/EggHeads) — **только при платной подписке с доступом к их API**; без подписки источник исключён из автопайплайна, `коэф_тренда_категории` (раздел 5.6) считается только по собственной истории | еженедельно/ежемесячно | категория, дата, индекс_роста_категории_рынок, кол-во_конкурентов, средняя_цена_категории |
| Вордстат | API Яндекс.Директа (подключим при появлении доступа) | периодически | keyword, дата, регион, частотность |
| Google Trends | `pytrends` — доступен уже сейчас | периодически | keyword, дата, регион, индекс_популярности (0-100) |
| Клики Яндекс.Директ на карточки своего сайта | Yandex Direct Reports API (`POST` → TSV, тип отчёта `AD_PERFORMANCE_REPORT`/`CUSTOM_REPORT`, группировка по `AdId`). Сопоставление `AdId → product_model_id` — **собственная таблица**, заполняется при создании кампании/объявления (Директ не отдаёт посадочную страницу как поле статистики по умолчанию — она либо через отдельный вызов `Ads.get`, либо, надёжнее, известна нам сразу, раз мы сами настраиваем кампании) | ежедневно | ad_id, дата, клики, показы, CTR, расход → джойн с product_model_id по своей таблице |
| Яндекс.Метрика — аудитория кликнувших | Yandex Metrika Reporting API (`GET /stat/v1/data`), группировки `ym:s:ageInterval`/`ym:s:genderName`/`ym:s:regionAreaName`/устройство, метрика `ym:s:visits`, фильтр `filters=ym:pv:URL=@'<страница товара>'` — можно сразу ограничить конкретной страницей товара, не обязательно идти через Директ | еженедельно | product_model_id, дата, сегмент (пол/возраст/устройство/гео), доля_кликнувших |
| Профиль покупателей (эталон) | CRM AWER — внутренняя БД, не Excel-экспорт | периодически, при накоплении новых заказов | сегмент (пол/возраст/устройство/гео), доля_покупателей |
| Погода | **Open-Meteo API** — бесплатный, открытый, без ключа и авторизации, архив + прогноз | ежедневно | регион, дата, средняя_температура |
| Курс USD | ЦБ РФ, `https://www.cbr-xml-daily.ru/daily_json.js` (`Valute.USD.Value`) | ежедневно | дата, курс_USD |
| Акции (участие) | `GET /v2/promotions` (список акций) + `GET /v2/promotions/{promotionId}/products` (товары в акции) — API v2, найден повторным изучением документации (раздел 4 — прежде считался недоступным по устаревшей v1-документации). ⚠️ Точная схема ответа (есть ли разбивка «цена за счёт продавца» отдельно от общей скидки) не подтверждена — проверить на этапе реализации | ежедневно | sku_id, promotionId, флаг_акции, цена_по_акции (состав уточнить) |

**Примечание по токенам API:** TTL 15 минут / 900 секунд (Lamoda Seller JSON-RPC), 24 часа (B2B Platform REST) — модуль забора данных должен сам обновлять токен.

**Нотификации (вебхуки) — ускоритель доставки событий, не замена опроса.** ⚠️ Исправление к прежней формулировке: источником истины по заказам официально остаётся **поллинг** (`GET /v2/orders` по `updatedAtFrom`) — вебхуки не гарантируют доставку и порядок, пропуски нужно закрывать регулярным опросом за период. Архитектура: вебхук ускоряет реакцию (не ждать до следующего планового опроса), но каждое событие подтверждается запросом `GET /v2/orders/{orderId}` (используется `data.id` из тела вебхука, например `"CZ117391950"` — не `trackingId`, это разные поля) и обрабатывается идемпотентно по ключу "id заказа + статус". Регулярный полный опрос по `updatedAtFrom` продолжает работать параллельно как страховка от пропущенных/недоставленных вебхуков.
```
statusChanged, itemStatusChanged          → ускоряют реакцию на продажи/возвраты,
                                             но не отменяют регулярный опрос /v2/orders
moderationApproved, moderationRejected    → ускоряют получение результата модерации цены
                                             (раздел 6, "ценовой карантин")
fulfilmentShipmentValidationRest          → ускоряет получение результата валидации поставки FBO
                                             (раздел 5.4, fbo_shipments)
```
Требования к эндпоинту (наша сторона): HTTPS, URL ≤1024 символов, доступен 24/7, отвечает <5 секунд, POST-обработчик по `type` события, опционально Basic Auth (`none` по умолчанию). **Подписка настраивается не самостоятельно через API, а через менеджера (KAM) или поддержку** — предоставляем URL эндпоинта (и логин/пароль для Basic Auth, если нужен), Lamoda сама включает нужные события. Без активной подписки на конкретный тип события вебхуки этого типа не приходят вообще, даже если эндпоинт готов принимать все типы.

**Возвраты для FBO — только через статус товара, не через отдельный метод.** `GET /v2/fbs/return-boxes`/`return-items` существуют, но только для модели FBS — для FBO (наш случай) единственный путь отследить возврат — статус `Returned` в `items[].status` (пример из вебхука `itemStatusChanged`, раздел 4) через опрос или вебхук заказов. Это подтверждает уже заложенный в разделе 4 источник возвратов, не меняет его.

**Практические ограничения API (из официального FAQ и документации по авторизации):**
- ⚠️ **Суммы — целые числа в минорных единицах валюты** (копейки для RUB/BYN, тиыны для KZT), не рубли с копейками как десятичная дробь. Модуль забора данных делит на 100 при приёме **каждого** денежного поля из API — цены, скидки, суммы заказов. Касается всех таблиц с `NUMERIC(x,2)` в рублях (margin_calc, order_items, promo_report_*, price_elasticity и т.д.) — конвертация на границе приёма данных, не внутри бизнес-логики.
- Пагинация: у заказов/отгрузок/возвратов — по умолчанию 100, максимум 1000; у номенклатур/минимальных цен — максимум 25 (без отдельного дефолта). Смотреть `meta` (`total`/`page`/`limit`/`totalPages`) в ответе, не считать вручную.
- `GET /v2/nomenclatures` требует обязательный параметр `country` (`RU`/`BY`/`KZ`) — без него `400`.
- Неизвестный query-параметр API молча игнорирует, не возвращает ошибку — источник тихих багов при опечатке в имени параметра.
- Официальных лимитов частоты запросов нет, но при `429`/`503` — экспоненциальный backoff; батчи держать ≤100 элементов, между запросами — пауза.
- `client_id`/`client_secret` + `sellerId` — общие для всех версий API, но нужны все три (не только ключи). `403 FORBIDDEN` чаще всего означает несовпадение `sellerId` с тем, под который выдан токен.
- Единый формат ошибок: `{"error": {code, message, details?}}` — коды `VALIDATION_FAILED`(400) / `UNAUTHORIZED`(401) / `FORBIDDEN`(403) / `NOT_FOUND`(404) / `TOO_MANY_REQUESTS`(429) / `SERVICE_UNAVAILABLE`(503) / `INTERNAL_SERVER_ERROR`(500) — единообразная обработка ошибок в модуле забора данных.
- Обновление токена — два правила одновременно: проактивно за 30-60 секунд до истечения `expires_in` (не хардкодить время жизни константой — брать из ответа `POST /v2/auth-token` каждый раз) **и** реактивно по факту `401` (перевыпустить токен и повторить исходный запрос один раз).
- Тело `POST /v2/auth-token` — единственное исключение в snake_case (`grant_type`/`client_id`/`client_secret`); везде в остальном API — camelCase.
- **Переотправка пропущенных нотификаций** — `POST /api/v1/notifications/resend`, тело `{"trackingIdCollection": [...]}`, максимум 100 номеров заказов за раз, код `201`. Переотправляет **все** сохранённые нотификации заказа в хронологическом порядке без фильтра по типу события — дубли гарантированы, идемпотентность (уже заложена, раздел 10) обязательна. Только для заказов (`statusChanged`/`itemStatusChanged`) — поставки FBO так не переотправляются, у них свой идентификатор. Повторный запрос по тому же заказу, пока предыдущий ещё выполняется, временно блокируется.

⚠️ **Исключено из автоматизированного пайплайна (нет API, только ручная выгрузка из ЛК):** отчёт «Воронка продаж» (просмотры/корзина на уровне модели — без замены, полностью теряем этот сигнал, раздел 5.3). Акции — **больше не в этом списке**: найден `/v2/promotions` в API v2 (см. таблицу выше), возвращены в автопайплайн после того, как считались недоступными по устаревшей v1-документации.

**Открытый вопрос к Lamoda:** уточнить у `api-integration@lamoda.ru`, есть ли API-эквивалент отчёта «Воронка продаж» (сейчас только ручная выгрузка, исключён из автопайплайна — акции больше не в этом списке, найдены в `/v2/promotions`), точную схему ответа `/v2/promotions/{promotionId}/products` (есть ли разбивка цены за счёт продавца), и формулу компенсации скидки на комиссию для ставки НДС 5%.

---

## 5. Расчётные сущности

⚠️ **Прогноз спроса — многоканальный, не только Lamoda.** Разделы 5.2-5.3 (классификация, прогноз) ведутся раздельно по каждой паре (`product_size`, `channel`) — тот же аппарат (SBC-классификация, TSB/CrostonSBA/сглаживание/бустинг/DeepAR, бэктест-турнир), просто прогоняется отдельно для Lamoda, WB, Ozon, собственного сайта: один и тот же товар может быть Smooth на WB (стабильные продажи, большая аудитория) и Lumpy на Lamoda (маленький объём, скачками) — разная аудитория, разное поведение, нет смысла считать одним прогнозом на все каналы сразу. `demand_forecast` получает поле `channel` (раздел 10), аналогично `margin_calc`.

**Раздел 5.1 (цензурирование по stockout) — механика специфична для Lamoda/FBO.** У WB/Ozon (FBS) и собственного сайта (раздел 4) нет отдельного склада площадки и, соответственно, нет инерции ранжирования конкретно на этой площадке при дефиците, как у Lamoda — дефицит для этих каналов означает "на ЦС закончилось", что сразу отражается на **всех** этих каналах одновременно (они делят один физический остаток), не на каждом отдельно. Право-цензурирование возвратами и общая логика "дни без остатка не считаются нулевым спросом" всё равно применимы — просто корректирующее событие общее сразу для всех, не по одному.

**Раздел 5.5 (точка заказа) — закупка у поставщика по агрегированному спросу со всех каналов.** ЦС питает не только Lamoda (через FBO-поставки), но и WB/Ozon напрямую (FBS — отгрузка с ЦС по каждому заказу), плюс собственный сайт (awer-russia.ru) и розницу. Точка заказа и страховой запас на ЦС (раздел 5.5 ниже) считаются от **суммарного** спроса по всем каналам, тянущим с одного физического остатка — не только от прогноза для Lamoda. Раздел 6, страница «Закупка у поставщика» — использует этот агрегированный спрос; страница «Поставка на FBO» — только Lamoda-специфичная часть (см. выше).

### 5.1 Временной ряд спроса (censored demand correction)

Дни с нулевым остатком на FBO не считаются нулевым спросом — исключаются из обучения или заменяются оценкой по соседним периодам.

```
net_спрос(t) = Delivered(t) − возвраты_с_лагом(t)
```

**Право-цензурирование возвратами:** последние `N` дней истории (`N = медиана lead_возврат по категории`, раздел 5.4) исключаются из обучающей выборки — за эти дни ещё не все возвраты успели прийти, значит `net_спрос` завышен. Решение: последний "рабочий" день истории для обучения = сегодня − N, а не сегодня.

**Хвост восстановления после OOS:** после возврата товара в наличие продажи не сразу выходят на норму — несколько дней действует инерция алгоритма ранжирования Lamoda (карточка была понижена в выдаче на время stockout) и поведенческая инерция покупателей. Дни в окне `recovery_tail_days` (3–14, задаётся по категории) сразу после восстановления остатка помечаются `is_recovery_tail_corrected = true`, наблюдаемый спрос делится на `recovery_tail_coefficient` (0.3–0.7) перед использованием в обучении — иначе первые дни после пополнения занизят прогноз, система примет вялые продажи за новую норму.

**Риск на будущее (не реализуем сейчас, держим в уме):** наивное исключение дней stockout без реконструкции значения может создать spiral-down эффект — заниженный прогноз → недостаточный запас → снова заниженный прогноз, по кругу. Если после запуска увидим систематическое занижение у часто дефицитных SKU (сверяем через `forecast_accuracy_log`, раздел 5.8) — путь апгрейда: Tobit Exponential Smoothing (TETS), встраивающий цензурирование прямо в модель сглаживания вместо постфактум-коррекции.

### 5.2 Классификация SKU

- **новинка** (< 60 дней истории) → прогноз по аналогам (см. 5.6)
- **активный** (есть история) → модель прогноза зависит от прерывистости спроса (см. ниже)
- **затухающий** (устойчиво падающие продажи, подтверждается `/v2/stocks/illiquid`) → консервативный прогноз, пополнение не считаем

**Минимальная история для доверия сезонной компоненте.** Порог "новинка" (< 60 дней) — про наличие истории вообще. Отдельно: сезонная составляющая прогноза (раздел 5.3) при истории короче одного полного сезонного цикла ненадёжна — модель ещё не видела, как категория ведёт себя в разные сезоны. До накопления 1-2 полных сезонных циклов истории по категории сезонная компонента берётся с сезонной группы (см. ниже), а не с собственного, ещё не показательного ряда SKU.

**Сезонные группы — не по категории, а по паттерну сезонности.** Разные товарные категории могут иметь одинаковую форму сезонной кривой (например, у лёгких летних вещей пик в одни и те же месяцы независимо от того, футболка это или шорты) — объединяем в группы по паттерну, а не считаем сезонность отдельно на каждую категорию:
```
seasonal_group: назначается вручную при заведении товара, либо кластеризацией
                исторических помесячных профилей продаж (похожая форма кривой → одна группа)
```
Сезонные коэффициенты (раздел 5.3) считаются на уровне `seasonal_group`, а не `category`. Категория остаётся отдельным измерением — используется для ABC×XYZ, воронки, маржи (разделы 5.2, 5.7) — но не диктует сезонность напрямую.

**Сезонный индекс из долгой истории (WB/Ozon/розница, раздел 4) — приоритетнее короткой истории Lamoda, пока своя не накопилась.** Форма сезонной кривой обычно похожа между каналами (пик в одни и те же месяцы), даже если абсолютный объём разный:
```
для каждой seasonal_group:
  сезонный_индекс(месяц) = среднее(продажи в этом месяце за все годы истории WB/Ozon/розница)
                            / среднегодовое(продажи по всем месяцам)
                            # нормализовано так, что среднее по индексу за год = 1.0 —
                            # это форма кривой, не абсолютный объём
```
Используется вместо/поверх сезонной компоненты из собственной истории Lamoda, пока не накоплено 1-2 полных сезонных цикла (см. порог выше) — форма кривой известна из длинной истории задолго до того, как это можно увидеть по самой Lamoda.

**Классификация типа спроса (SBC-метод, Syntetos-Boylan-Croston) — две оси, не одна.** Раньше критерий был бинарным (только регулярность во времени). SBC добавляет вторую ось — изменчивость объёма — и даёт 4 категории, каждая со своим методом прогноза и своим буфером:
```
ADI (Average inter-Demand Interval) = дни в периоде / дни с ненулевым спросом
CV² (Squared Coefficient of Variation) = (σ/среднее)² по ненулевым продажам

Плавный (Smooth):          ADI < 1.32, CV² < 0.49  → обычная модель (сглаживание + сезонность), обычный буфер
Неустойчивый (Irregular):  ADI < 1.32, CV² >= 0.49 → обычная модель прогноза; буфер шире автоматически
                                                        (через σ_ошибки_прогноза, раздел 5.5 — не отдельный множитель)
Прерывистый (Intermittent): ADI >= 1.32, CV² < 0.49 → TSB/CrostonSBA, обычный буфер для этого класса
Неравномерный (Lumpy):     ADI >= 1.32, CV² >= 0.49 → TSB (не CrostonSBA — точнее на этой категории
                                                        по эмпирическому сравнению); буфер шире автоматически —
                                                        самый сложный случай: непредсказуемы и время,
                                                        и объём одновременно
```
Многие размеры внутри модели (особенно крайние — например, самый большой размер куртки) попадают в Intermittent или Lumpy — обычное сглаживание здесь даёт смещённую оценку. Реализуется через `statsforecast.models.TSB` / `CrostonSBA` (раздел 7 — не пишем с нуля):

⚠️ **Не путать с XYZ-классификацией выше.** XYZ считается на уровне модели, обычным CV (не в квадрате), пороги 10%/25%, и определяет z/частоту пересмотра. SBC CV² считается на уровне размера, в квадрате, порог 0.49, и определяет **метод прогноза** (не буфер напрямую — буфер по-прежнему считает раздел 5.5 через σ_ошибки_прогноза, которая естественно окажется шире у Irregular/Lumpy сама по себе, без дополнительного ручного множителя поверх).
```
При каждом ненулевом спросе d(t):
  ẑ(t) = ẑ(t-1) + α × (d(t) − ẑ(t-1))       # сглаженный размер спроса
  p̂(t) = p̂(t-1) + β × (интервал − p̂(t-1))   # сглаженный интервал между продажами
прогноз = (1 − β/2) × ẑ(t) / p̂(t)            # поправка Syntetos-Boylan (SBA) на смещение;
                                              # TSB дополнительно обновляет вероятность спроса
                                              # даже в дни без продаж — точнее на Lumpy-категории
```
Перед расчётом ADI/CV² — фильтр выбросов (3σ, уже заложен в разделе 5.5) — большие разовые продажи иначе завышают CV² и искажают классификацию.

Для Smooth (обычные "активные" SKU) — обычная модель, как раньше.

**Выбор метода через бэктест — не жёсткое правило, а проверка на истории.** SBC-классификация выше — только начальный кандидат, не финальное решение. При каждом плановом пересмотре (частота — по сегменту ABC×XYZ, таблица выше) для каждого `product_size` прогоняются кандидаты (сглаживание, `TSB`/`CrostonSBA`, наивный сезонный — как базовая линия для сравнения, и для SKU с достаточной историей и признаками цены/промо — градиентный бустинг, `XGBoost`/`LightGBM`/`CatBoost`, с `monotone_constraints` по признаку цены — гарантирует, что рост цены в прогнозе не увеличивает спрос, экономически осмысленный результат вместо возможного шума модели без ограничений) на holdout-окне последних периодов, и выбирается метод с наименьшим WAPE на этом окне:
```
для каждого кандидата (кроме DeepAR): обучить на истории до T, прогноз на holdout [T, T+период]
WAPE_кандидата = Σ|факт − прогноз| / Σ факт за holdout
model_type = argmin(WAPE_кандидата)
```
**DeepAR (`GluonTS`, open source, Apache 2.0, бесплатно) — отдельный, шестой кандидат, архитектурно не такой, как остальные.** Не обучается заново на каждый `product_size` отдельно — одна общая модель обучается сразу на всём каталоге (все ряды вместе), затем выдаёт прогноз для каждого ряда. Включается в тот же бэктест-турнир по тому же критерию WAPE на holdout, но обучается **один раз за цикл пересмотра**, не по разу на кандидата на SKU — вычислительно легче, чем кажется: реалистично обучается на CPU (без GPU) при масштабе AWER (сотни-тысяча рядов) за время порядка часа, укладывается в тот же еженедельный цикл. Дополнительный плюс — совместное обучение на всех рядах даёт естественный перенос знаний на новинки без истории (раздел 5.6) — можно использовать как альтернативный/сверочный источник прогноза новинки наравне с аналог-based newsvendor-подходом, не только для активных SKU.

Смена метода логируется в `model_recalibration_log` с `triggered_by = 'backtest'` — та же таблица, что уже используется для override по MAPE-тревоге (раздел 5.8, `triggered_by = 'auto'`/`'manual'`), просто третья причина записи.

**ABC-классификация** — комбинированный скор по выручке и по количеству (не одна метрика):

| Класс | Критерий |
|---|---|
| A — ходовые | топ по комбинированному скору (выручка + кол-во) |
| B — средние | основная масса |
| C — хвост | низкие продажи |
| затухающие | устойчиво падающие |

**XYZ-классификация** — по стабильности спроса, независимо от ABC (важность ≠ предсказуемость). Считается на уровне модели — `net_спрос` суммируется по всем размерам за день, затем CV берётся от полученного ряда; это отдельный сигнал от `demand_pattern` (SBC-классификация выше — та считается на уровне размера и решает, какой метод прогноза использовать для конкретного размера, а не какой буфер держать для модели в целом):
```
CV = σ(net_спрос) / среднее(net_спрос) × 100%
X: CV < 10%   — стабильный спрос
Y: CV 10–25%  — умеренные колебания
Z: CV > 25%   — нерегулярный, трудно прогнозируемый спрос
```

**Комбинированная матрица ABC×XYZ** — определяет и z (сервис-уровень), и частоту пересмотра плана:

| Сегмент | z (сервис-уровень) | Частота пересмотра |
|---|---|---|
| AX | 1.88 (97%) | ежедневно/еженедельно |
| AY | 1.75 (~96%) | еженедельно |
| AZ | 1.65 (95%) | еженедельно + ручная проверка перед закупкой |
| BX | 1.65 (95%) | раз в 2 недели |
| BY | 1.5 (~93%) | раз в 2 недели |
| BZ | 1.28 (90%) | раз в 2 недели + ручная проверка |
| CX | 1.28 (90%) | раз в месяц |
| CY | 1.28 (90%) | раз в месяц |
| CZ | автопополнение не считаем — закупка только вручную по триггеру | по триггеру отклонения |
| затухающие | пополнение не считаем | — |

### 5.3 Прогноз спроса

```
прогноз_спроса = f(net_спрос, сезонность, флаг_акции, температура,
                    сигнал_каннибализации, коэф_интерес)
```

**Разделение на базовый спрос и промо-аплифт.** `флаг_акции` сейчас просто один из входов общей функции — смешанный прогноз не показывает, что было бы без акции. Разделяем на два явных числа:
```
базовый_спрос = прогноз при флаг_акции = 0 (контрфактический — "если бы акции не было")
промо_аплифт  = факт_за_период_акции − базовый_спрос
```
Используется как честная база для `margin_calc` (какой объём продался бы по обычной цене) и для решения "продлевать ли акцию" — сравниваем аплифт с потерей маржи от скидки напрямую, а не гадаем по смешанному числу.

**Каннибализация цветов/размеров — формализована через кросс-эластичность.** При обнулении остатка одного цвета/размера — проверяем прирост продаж по соседним цветам/размерам того же артикула за тот же период. Вместо разовой эвристики — та же регрессия, что для собственной эластичности (раздел 5.10), только между парой связанных SKU:
```
кросс_эластичность(A, B) = ∂ln(спрос_B) / ∂ln(доступность_A)   # доступность = 1 при наличии, 0 при stockout
                                                                # оценивается по историческим эпизодам stockout A
```
Даёт количественную оценку перетёкшего спроса (`demand_shift_qty`), а не только флаг "событие было" — можно сразу использовать в прогнозе соседнего SKU, а не только логировать факт.

**Промо-каннибализация между разными артикулами — второй, отдельный механизм.** В отличие от предыдущего (замена цвета/размера при дефиците внутри одного артикула) — здесь речь о том, что акция на **один** артикул уводит покупателей от **другого**, конкурирующего артикула (например, две похожие рубашки в одной категории), продающегося по обычной цене. Обнаруживается статистически, не эвристикой:
```
D = Snp − Sp
Snp — средние непромо-продажи артикула A, когда артикул B НЕ в акции
Sp  — средние непромо-продажи артикула A, когда артикул B в акции

t_stat, p = двухвыборочный_t_test(продажи_A_при_B_без_акции, продажи_A_при_B_с_акцией)
если p < 0.05 и D > 0 → пара статистически значимо каннибализирует друг друга
```
Прогоняется по всем парам моделей внутри категории (не обязательно одного артикула). Требует точной разметки акций — источник `GET /v2/promotions` (раздел 4); неразмеченные скидки (акции вне этого API, если такие есть) дают ложноположительные и ложноотрицательные срабатывания.

**Blending интереса (Вордстат + Google Trends + клики Директа).** Вордстат и Google Trends — не взаимозаменяемы, а два независимых замера поискового интереса (разные поисковики, разная база пользователей), усредняются в единый источник:
```
коэф_поискового_интереса = среднее(коэф_вордстат, коэф_google_trends) — если доступны оба;
                            иначе — тот, что доступен (сейчас обычно только Google Trends, т.к.
                            Вордстат ждёт доступа к API Директа)
```
Клики по объявлениям на карточку своего сайта — сигнал точнее общего поискового интереса: это интерес к конкретной модели, а не к фасону/категории в целом, и часто доступен раньше, чем карточка вообще появляется на Lamoda (если рекламная кампания на сайте стартует до листинга на маркетплейсе).
```
коэф_direct = темп_роста(клики на product_model_id) / темп_роста(клики у аналога в его запуск)

качество_аудитории(t) = среднее по 4 измерениям (пол, возраст, устройство, гео) от:
                         [1 − (Σ_value |доля_кликнувших − доля_покупателей| внутри измерения) / 2]
                         # считается отдельно на каждом измерении, потом усредняется — иначе гео
                         # с десятками городов задавит бинарный пол просто числом слагаемых
                         # без эталона по измерению — оно выпадает, среднее берётся по оставшимся
                         # без эталона вообще (нет данных CRM) — качество_аудитории = 1 (не корректируем)

коэф_direct_скорректированный = коэф_direct × качество_аудитории(t)
                         # клики от аудитории, не похожей на реальных покупателей, взвешиваются ниже —
                         # сигнал по кликам сам по себе зашумлён, если кликает не та аудитория, что покупает

коэф_интерес = 0.6×коэф_direct_скорректированный + 0.4×коэф_поискового_интереса
```
⚠️ **Без компонента "воронка Lamoda"** — отчёт «Воронка продаж» без API-доступа исключён из автопайплайна (раздел 4). Раньше вес воронки рос по мере набора статистики после публикации карточки (до 85% в стабильном режиме) — теперь коэф_интерес держится на внешних сигналах (клики + поиск) весь жизненный цикл SKU, без снижения точности со временем за счёт внутреннего сигнала Lamoda. Веса стартовые, калибруются регрессией по накоплению истории. Если для конкретной модели рекламы в Директе не было — компонент выпадает, весь вес уходит на `коэф_поискового_интереса`.

**Anomaly-детекция без данных воронки.** Правило "падение конверсии в корзину = проблема карточки" (требовало просмотров/добавлений в корзину) неприменимо без API-доступа к воронке. Замена, доступная через `/v2/orders`: рост доли отменённых/недоставленных заказов (`Not delivered`/`Canceled` среди `items[].status`) — более грубый, но автоматизируемый индикатор проблемы с карточкой/логистикой, не спросом.

### 5.4 Состояния товара и lead time

```
поставщик → остаток_1С → в пути на FBO → остаток_FBO → в пути к клиенту
                                                             │         │
                                                         Delivered  возврат
                                                         (продано)     │
                                                                  остаток_FBO (снова)
```

```
lead_поставка_FBO = медиана(дата_приёмки_FBO − дата_создания_поставки)
lead_возврат       = медиана(дата_возврата_в_остаток − дата_статуса_возврата), по категории
lead_поставщик     = срок_производства + срок_доставки (из справочника поставщика)
```
`fbo_shipments.status` (раздел 10) обновляется вебхуком `fulfilmentShipmentValidationRest` (раздел 4) сразу по результату валидации поставки Lamoda — не только по факту появления товара в `stock_fbo`, что раньше было единственным сигналом.

### 5.5 Точка заказа и рекомендация пополнения

```
прогноз_остатка(T) = остаток_FBO
                    + в_пути_на_FBO (ожидаемые к приёмке ≤ T)
                    + в_пути_от_клиента × доля_с_lead_возврат_≤_T
                    − прогноз_продаж(до T)

точка_заказа = прогноз_спроса(на L) + страховой_запас
```

**Незащищённое окно L — не только срок доставки.** Между плановыми пересчётами вмешаться в остаток нельзя, значит окно шире, чем время доставки:
```
L = lead_поставка_FBO (или + lead_поставщик, если нужна закупка с нуля) + интервал_пересмотра
```
`интервал_пересмотра` — из частоты пересмотра сегмента ABC×XYZ (раздел 5.2, таблица review_frequency, в днях). У товаров с редким пересмотром (классы C/Z — раз в месяц) это увеличивает L существенно, а расчёт без этого слагаемого систематически занижает буфер именно там.

**Базовая формула страхового запаса:**
```
страховой_запас = z × σ_ошибки_прогноза × √L
```
`σ_ошибки_прогноза` — стандартное отклонение **разницы факт − прогноз** (из `forecast_accuracy_log`, раздел 5.8), а не сырого `net_спрос`. Сезонность, тренд и промо-эффект уже учтены в прогнозе (раздел 5.3) — если взять σ от сырых продаж, эти эффекты задвоятся в буфере. Дни stockout (`is_stockout_corrected`) и выбросы за пределами 3σ (разовая оптовая продажа/тендер) исключаются из расчёта σ — иначе буфер раздувается на месяцы вперёд из-за одного нетипичного дня.

**Расширенная формула — при нестабильном сроке поставки:**
```
страховой_запас = z × √(L × σ_ошибки² + d² × σL²)
```
`σL` — стандартное отклонение фактического lead time (`lead_time_stats.std_days`, `lead_type = 'supplier'`, раздел 5.9), `d` — средний дневной спрос. Применяется вместо базовой формулы, когда `on_time_delivery_rate` поставщика (раздел 5.9) заметно ниже 100% — до сих пор мы просто увеличивали L "на глаз", теперь нестабильность входит в формулу явно. Если сроки скошены (обычно вовремя, изредка сильно опаздывает) — вместо симметричной σL берётся эмпирическая надбавка: 95-й перцентиль фактических сроков минус средний срок (это честнее, не занижает).

**Для прерывистого спроса (раздел 5.2, Croston-ветка)** — не масштабировать через `√L`: множитель корня и вносит основную ошибку на рваных позициях, потому что нулевые дни ломают предпосылку о независимости. Вместо этого — σ считается сразу по скользящим суммам спроса за окна длины L (не по дневному ряду), без последующего умножения на `√L`.

Полный lead time (компонент `L`, без интервала пересмотра) = `lead_поставка_FBO`, если товар есть на 1С, либо `lead_поставщик + lead_поставка_FBO`, если нужна закупка с нуля.

Если `доступен_к_заказу_у_поставщика = false` — рекомендация закупки не формируется, вместо неё — предупреждение "SKU без пополнения, идёт на исчерпание остатка".

### 5.6 Новинки — первая поставка

**Новый цвет существующего артикула — отдельный случай, точнее совсем новой модели.** Если запускается новый цвет артикула, который уже продаётся в других цветах — есть собственная история по артикулу в целом, просто не по этому конкретному цвету. Вместо подбора аналогов среди чужих моделей (как для полностью новой модели) — распределяем свою же историю:
```
прогноз_по_артикулу = агрегат по всем product_model (цветам) этого артикула, раздел 5.3-5.5

прогноз_нового_цвета = прогноз_по_артикулу × доля_цвета
доля_цвета — из норматива распределения по цвету в категории (color_profile), перемножается
             с размерным профилем так же, как раздел размерной раскладки ниже
```
Точнее чужих аналогов, потому что это ваш собственный спрос на артикул — просто ещё не разложенный по конкретному новому цвету.

**Новинка на Lamoda с историей на другом канале (WB/Ozon/розница, раздел 4) — третий, приоритетный случай.** "Новинка" по Lamoda-данным не значит "новый товар вообще" — если артикул уже продаётся годами на WB/Ozon/рознице, это не задача холодного старта, а задача пересчёта известного спроса на новый канал:
```
если есть история товара на другом канале ≥ 1 год:
  канал_коэффициент = среднее(Lamoda_продажи / продажи_канала) по товарам, которые продаются
                       на обоих каналах одновременно, за перекрывающийся период
                       # калибруется по факту на паре товаров с обоими каналами, не константа
                       # с потолка — у разных категорий соотношение может быть разным

  прогноз_новинки_по_истории_канала = продажи_канала_за_аналогичный_период_года × канал_коэффициент
```
Приоритетнее аналог-based метода ниже, когда доступен — это буквальная история продаж того же товара, не оценка по похожести на другие товары. Аналог-based метод (ниже) — резерв на случай, когда ни у одного канала нет истории вообще (действительно новая модель).

**Недооценённый потенциал — товар уже на Lamoda, но продаётся заметно хуже, чем предсказывает канал-коэффициент.** Отдельный от прогноза новинок случай: не "сколько закупить впервые", а "это правда нет спроса на Lamoda, или проблема с листингом (карточка/цена/категория/видимость), которую стоит проверить, прежде чем списывать товар как непопулярный на этой площадке":
```
ожидаемые_продажи_Lamoda = продажи_на_канале_за_период × канал_коэффициент(категория)

если факт_продажи_Lamoda << ожидаемые_продажи_Lamoda (напр. факт < 20% от ожидаемых)
   и товар в наличии на FBO весь период (не сам дефицит — причина низких продаж):
   флаг "недооценённый потенциал" — приоритет для проверки менеджером карточки/цены/категории,
   не автоматическое решение прекратить закупку
```
Не заменяет суждение менеджера — это фильтр, какие товары стоит посмотреть в первую очередь, среди всех, что продаются на Lamoda слабее ожидаемого. Флаг снимается, если после проверки причина объективная (например, категория и правда хуже заходит на аудитории Lamoda, не проблема листинга).

**Диагностический чек-лист при срабатывании флага — часть автоматически, часть только вручную:**

*Проверяется автоматически (данные уже в системе):*
```
1. Полнота атрибутов карточки — GET /v2/nomenclatures/{sku}/attributes (раздел 8.1),
   сравнить с полной карточкой той же категории; пустые атрибуты бьют по видимости в фильтрах
2. Наличие именно по ходовым размерам, не "хоть что-то есть" — сверить размеры, дающие
   основные продажи на канале-источнике (size_profile/external_channel_sales_history),
   реально ли они в наличии на FBO, а не только нестандартный неходовой размер
3. Цена относительно категории (category_market_trends.avg_category_price) и относительно
   цены на канале-источнике за тот же период — заметно дороже без видимой причины = кандидат
4. Статус карточки — не на модерации ли, нет ли проблем с маркировкой/сертификацией
5. Акции у конкурентов в категории (GET /v2/promotions, раздел 4) — конкуренты в акции,
   мы нет — может объяснять разницу без проблемы с самой карточкой
```
*Требует ручной проверки — честная граница возможностей без API:*
```
6. Фактическая видимость в поиске / позиция в ранжировании Lamoda — то, что закрыла бы
   «Воронка продаж» (просмотры/показы), но она без API, исключена из автопайплайна (раздел 4).
   Проверяется только вручную — зайти на сайт Lamoda и посмотреть позицию по релевантным
   запросам, как обычный покупатель
7. Отзывы/рейтинг товара — отдельного API-метода в изученной документации не встретили,
   тоже вручную через ЛК
```
Пункты 1-5 система может подсветить сама рядом с флагом (что именно похоже на проблему); пункты 6-7 — только напоминание менеджеру проверить, не автоматизируемая часть.

**Прогноз (для полностью новой модели без истории ни по артикулу, ни на других каналах):**
```
коэф_тренда_категории = f(собственная_динамика_категории, индекс_роста_категории_рынок)
                         # собственная — по вашей истории продаж в категории (было и раньше)
                         # рыночная — по конкурентной аналитике (раздел 4), вес выше, когда своей
                         # истории в категории мало или нет вообще (совсем новая для вас категория)

прогноз_по_аналогам = Σ(продажи_аналога_за_первые_N_недель × вес_сходства) / Σ(весов)
                      × коэф_тренда_категории

прогноз_новинки_финальный = прогноз_по_аналогам
                            × медиана_поправки_категории   (историческая точность прогноза
                                                              новинок именно в этой категории)
                            × коэф_поискового_интереса^вес   (вес < 1, модерирующий сигнал; Вордстат+Google
                                                                Trends вместе, раздел 5.3)
```

**Объём первой поставки** (тестовая партия, модель newsvendor — одноразовый заказ перед стартом продаж без права допоставки в короткий срок):
```
период_охвата = lead_поставщик + lead_поставка_FBO + 3-4 недели буфера

Cu (underage) = маржа_ед_по_плановой_цене        # раздел 5.7, формула маржи, но price_realized заменена на
                                                    product_models.planned_launch_price — margin_calc для новинки
                                                    ещё не существует (нет фактических продаж)
Co (overage)  = себестоимость_ед × (1 − liquidation_recovery_rate)  # потеря на единицу непроданного остатка;
                                                                       -- liquidation_recovery_rate — доля себестоимости,
                                                                       -- которую в среднем удаётся вернуть уценкой
                                                                       -- (параметр настроек, раздел 3, по умолчанию 0.5)

критическая_доля = Cu / (Cu + Co)
z_новинки = Φ⁻¹(критическая_доля)                 # квантиль нормального распределения

σ_объёма = прогноз_новинки_финальный(период_охвата) × σ_поправки_категории  # мультипликативно, не по дням —
                                                                              -- σ_поправки_категории это разброс
                                                                              -- коэффициента факт/прогноз по прошлым
                                                                              -- новинкам категории, не суточная
                                                                              -- волатильность, поэтому без √T

объём_первой_поставки = прогноз_новинки_финальный(период_охвата) + z_новинки × σ_объёма
```
`z_новинки` больше не фиксированное число — считается индивидуально по экономике каждого SKU: товар с высокой маржой и дешёвой ликвидацией при неудаче получает более высокий z (выгоднее перезаказать), товар с тонкой маржой и дорогой уценкой — низкий (выгоднее недозаказать).

Округление вверх до MOQ поставщика. Если расчётный объём заметно ниже MOQ — явный сигнал риска ("MOQ вынуждает завезти в X раз больше расчётной потребности").

**Погода** сдвигает не объём, а дату начала периода_охвата — аномально тёплая осень сдвигает старт вправо, холодная — влево.

**Размерная раскладка:** по собственному размерному профилю (раздел 4 — расчёт из истории заказов через API, не отчёт Lamoda — у него нет API).

**Контрольная точка пересчёта** — через 2-4 недели после старта продаж:
```
факт/прогноз = реальный_темп_продаж / прогноз_на_этот_период
```
- В коридоре (напр. 0.7–1.3) → SKU переходит в обычный сегмент ABC×XYZ (раздел 5.2)
- Сильно ниже → переоценка вниз, возможен переход в "затухающие"
- Сильно выше → досрочная закупка вне обычного цикла

### 5.7 Маржинальность и ценообразование

**Себестоимость:**
```
курс_фактический(t) = курс_ЦБ_текущий(t) + корректировка_курса   # напр. 85.3 + 4 = 89.3

себестоимость_ед(t) = закупка_USD × курс_фактический(t)
                     + доля_логистики_от_поставщика
                     + доля_пошлины/растаможки
                     + упаковка + маркировка (КИЗ)
```
`корректировка_курса` — редактируемый параметр (справочник настроек маржи), может быть и отрицательным. Отражает реальный курс покупки валюты, а не биржевой курс ЦБ напрямую.

⚠️ Курс берётся **текущий на дату расчёта, а не на дату закупки** — весь товар в ЦС учитывается в долларах, и переоценка идёт по всему остатку (1С + в пути + FBO), включая уже давно поставленные позиции, а не только по будущим закупкам. Это значит маржа пересчитывается ежедневно вместе с курсом ЦБ, а не только при разовых скачках. **Подтверждено:** 1С сама ведёт учёт товара в долларах по текущему курсу — значит расхождения с бухучётом нет, отдельная «учётная» маржа не нужна, цифра единая.

В выводе рекомендации по цене добавляется `причина_отклонения = курс / спрос / комиссия`, чтобы отличать курсовую переоценку от реальной проблемы со спросом или наличием товара.

**НДС (УСН доходы с НДС 5%):**
```
НДС_к_уплате = цена_реализации_с_НДС × 5 / 105
цена_без_НДС = цена_реализации_с_НДС − НДС_к_уплате
налог_УСН    = цена_без_НДС × 6%
```
⚠️ Уточнить у Lamoda формулу компенсации скидки на комиссию для ставки НДС 5% (для 10% есть множитель ×1.15, для 20%/без НДС — нет; официальные документы с точными формулами есть у Lamoda для ставок 20%, 10% и без НДС — но не для 5% (новая ставка для УСН-плательщиков), её отдельно нет в найденных материалах). **Решение:** закладываем как параметр `commission_vat_multiplier = 1` по умолчанию — после ответа Lamoda меняем одно значение в настройках, формулу переписывать не нужно.

**Маржа:**
```
маржа_ед = цена_реализации_с_НДС
         − себестоимость_ед
         − (цена_реализации_с_НДС × 0.42)   # комиссия Lamoda
         − логистика_FBO_на_ед
         − НДС_к_уплате
         − налог_УСН

маржа_% = маржа_ед / цена_реализации_с_НДС
```
⚠️ Для товаров в акции `цена_реализации` — это **«цена за счёт продавца»**, не цена, которую видит покупатель: скидка на маркетплейсе часто комбинированная (продавец + Lamoda). Источник — `GET /v2/promotions/{promotionId}/products` (API v2, раздел 4). Схема ответа не подтверждена детально — если там есть явная разбивка цены продавца отдельно от общей скидки, используем её напрямую; если нет (отдаёт только итоговую цену покупателя) — тогда fallback на `цена_факт` из `/v2/orders`, с тем же известным риском занижения маржи на долю Lamoda в скидке. Проверить на этапе реализации, какой случай верный.

**Промо-период → дневной расчёт.** `GET /v2/promotions/{promotionId}/products` (раздел 4) опрашивается ежедневно вместе с обычными продажами — `margin_calc` считается по факту каждого дня сразу, без задержки и без backfill (в отличие от прежней схемы с еженедельным Excel-отчётом). Если ответ API даёт только среднюю цену за период акции, а не по дням — тогда та же логика "одна цена на весь период акции", что была заложена для Excel-варианта, просто с ежедневным обновлением самого факта участия в акции, не только цены.

**Правила рекомендаций:**

| Ситуация | Рекомендация |
|---|---|
| маржа_% < 30% | повысить цену до мин. порога, либо приостановить закупку |
| маржа_% в 30–80% + неликвид (`/v2/stocks/illiquid`) | снизить цену / завести в акцию — ускорить оборот |
| маржа_% на нижней границе + неликвид | не трогать цену; списание/уценка как крайний случай |
| маржа_% > 80% + стабильный спрос | тестовое повышение цены — через A/B-подход (раздел 5.10), а не сразу на весь остаток |
| курс USD изменился (пересчёт ежедневный, т.к. себестоимость в ЦС валютная) | маржа всего активного ассортимента пересчитывается; отклонение помечается `причина_отклонения = курс`, чтобы не путать с проблемой спроса |

**Ежемесячная сверка с официальным отчётом.** Lamoda ежемесячно присылает через ЭДО «отчёт комиссионера» с полем «Цена реализации, руб. вкл. НДС» — официальная фискализируемая цифра, которую они называют базой расчёта. Не источник для автопайплайна (документ, не API) — но раз в месяц сверяем собственный расчёт `margin_calc` с этой цифрой, чтобы заметить накопленное расхождение (особенно ожидаемое там, где в дни акций мы использовали `цена_факт` вместо недоступной «цены за счёт продавца», раздел 5.7 выше).

**Маржинальность по WB и Ozon — параллельный расчёт, тот же принцип.** `margin_calc` обобщена на несколько каналов (поле `channel`, раздел 10) — тот же товар, тот же `cost_unit` из 1С (себестоимость канало-независима), но своя цена реализации и своя структура комиссии на каждой площадке:
```
маржа_% (канал) = (price_realized − cost_unit − commission_amount − logistics_fbo_amount − налоги) / price_realized

price_realized:      Lamoda — «цена за счёт продавца» (см. выше) / WB, Ozon — price_actual из
                      постоянного потока продаж (раздел 4), у них нет аналога «цены продавца»
commission_amount:    по channel_commission_rates (раздел 10) — WB: Tariffs API (комиссии по категориям,
                      меняются, отслеживать effective_from/effective_to); Ozon — точный метод не
                      подтверждён документацией, раздел 9 — на старте можно взять комиссию из отчёта
                      о продажах, если она там указана построчно
```
⚠️ Собственный сайт (awer-russia.ru) в эту маржинальность не входит — там нет комиссии маркетплейса, только эквайринг/платёжный шлюз, другая по природе структура затрат. Считается отдельно, если понадобится, не в рамках этого многоканального сравнения.
**Назначение — сравнение прибыльности одного и того же товара между каналами**, не отдельная система для каждой площадки: один и тот же запрос к `margin_calc` с фильтром по `product_size_id` и разными `channel` показывает, где товар выгоднее продавать прямо сейчас — тот же принцип, что уже частично используется в детекторе недооценённого потенциала (раздел 5.6), но здесь про маржу, не про объём продаж.

⚠️ Розница (раздел 4) в эту маржинальность не входит — там нет понятной комиссионной структуры маркетплейса, розничная маржа считается по-другому (или уже считается в 1С отдельно, не дублируем здесь).

### 5.8 Контроль точности прогноза (MAPE/WAPE, fill rate)

Раздел 5.6 даёт контрольную точку только для новинок. Системная метрика нужна для всего ассортимента — чтобы видеть, где модель систематически ошибается, а не только реагировать постфактум на нехватку/избыток.

```
MAPE = среднее(|факт − прогноз| / факт) × 100%
WAPE = Σ|факт − прогноз| / Σ факт × 100%
MAE  = среднее(|факт − прогноз|)              # абсолютная ошибка в штуках, сопоставимо между позициями
RMSE = √(среднее((факт − прогноз)²))          # с большим штрафом за крупные единичные промахи
средний_дефицит = среднее(max(факт − прогноз, 0))   # недопрогноз — упущенная маржа
средний_профицит = среднее(max(прогноз − факт, 0))  # перепрогноз — риск уценки
```
MAPE/WAPE/MAE/RMSE — симметричные метрики, считают недопрогноз и перепрогноз одинаково "плохими". Но экономически они разные: недопрогноз стоит упущенной маржи, перепрогноз — риска уценки (раздел 5.7, `liquidation_recovery_rate`). Дефицит/профицит разделяют это явно — если дефицит систематически выше профицита у конкретного сегмента, это сигнал, что буфер занижен (не проблема метода прогноза), и наоборот.
MAPE/WAPE усредняют ошибку по всему ассортименту — единичный крупный промах (каннибализация, всплеск от акции, хвост восстановления после OOS) тонет среди множества обычных дней. RMSE ловит именно такие случаи за счёт квадратичного штрафа — используется как дополнительный сигнал к порогам эскалации ниже, не вместо MAPE.

Считается регулярно по каждому `product_size_id` и агрегируется по сегменту ABC×XYZ (раздел 5.2) — сравнение точности между сегментами объективнее, чем абсолютные цифры без нормализации.

**Источник `forecast_qty` для новинок vs обычных SKU.** Пока SKU в статусе "новинка" (раздел 5.2, до контрольной точки пересчёта раздела 5.6) — `forecast_qty` берётся из `new_item_size_allocation.allocated_qty`. После контрольной точки, когда SKU переходит в обычный сегмент ABC×XYZ, источник переключается на `demand_forecast.forecast_qty`. Переключение источника — по тому же событию, что и смена статуса SKU (раздел 5.6), не отдельная логика.

**Fill rate — вторая метрика, отдельно от z.** Наш `z` (раздел 5.2/5.5) — это cycle service level (Type 1): вероятность не уйти в дефицит за цикл пополнения, бинарный исход. Он используется для расчёта буфера — это нормально. Но для мониторинга результата важнее **fill rate (Type 2)** — доля фактически закрытого спроса, а не факт "был дефицит хоть раз":
```
fill_rate = Σ выполненный_спрос / Σ весь_спрос_за_период × 100%
```
Fill rate считается по факту (не для расчёта буфера, а для проверки), потому что честнее показывает реальные потери выручки — SKU может быть "в дефиците всего 2 дня" (низкий урон по cycle service level), но потерять там 80% ожидаемого спроса пикового периода (сильный урон по fill rate).

⚠️ Оговорка: знаменатель ("весь спрос") — это тот же реконструированный `demand_timeseries` (раздел 5.1), что участвует в обучении прогноза. Fill rate здесь не полностью независимая проверка, а согласованная с моделью метрика — полезна для сравнения между SKU/периодами, но не как абсолютная истина о "реальном" спросе.

**Пороги отклонения и эскалация — что делать с MAPE после того, как его посчитали.** Сама по себе цифра MAPE бесполезна без реакции на неё:
```
MAPE < warning_threshold (напр. 20%)                     → status = норма
warning_threshold ≤ MAPE < critical_threshold (напр. 40%) → status = предупреждение
MAPE ≥ critical_threshold                                  → status = критично
```
Пороги — параметры настроек (по категории, т.к. у категорий разная естественная волатильность — раздел 5.2, XYZ). Реакция разная по статусу:
- **предупреждение**, удерживается `consecutive_periods_trigger` периодов подряд (напр. 3) → помечается как систематическое отклонение, не разовый шум. Кандидат на смену метода прогноза для этого SKU (например, перевести на Croston/SBA, если модель систематически промахивается на фактически прерывистом спросе — раздел 5.2), с логированием смены
- **критично** → сразу эскалация в агент триажа аномалий (раздел 8.2) — резкое расхождение чаще говорит о внешнем факторе (сорванная поставка, всплеск/провал спроса, ошибка в данных), а не о постепенном дрейфе модели, разбираться нужно быстрее одного цикла

**Приоритет правил выбора модели:** SBC-классификация по ADI×CV² (раздел 5.2, Syntetos-Boylan-Croston) — это дефолтное правило, пересчитывается каждый цикл заново. Запись в `model_recalibration_log` — override поверх дефолта, действует до следующего пересчёта дефолтного правила; если после override классификация снова меняется — дефолтное правило выигрывает автоматически, override не "залипает" навсегда молча.

**Степень достоверности рекомендации — отдельный индикатор для страницы поставок (раздел 6), в процентах.** Не то же самое, что WAPE сам по себе — композитный сигнал из нескольких независимых факторов, каждый из которых может подрывать доверие к цифре по своей причине:

```
достоверность_% = 100
  − штраф_WAPE                    # масштабируется от WAPE последнего периода/бэктеста —
                                   # напр. min(WAPE_%, 40) — на этой шкале WAPE 40%+ уже
                                   # снимает все 40 пунктов, дальше не усугубляет
  − штраф_паттерна                # demand_pattern (раздел 5.2): Smooth=0, Irregular=5,
                                   # Intermittent=10, Lumpy=20 — фиксированные, не от WAPE
  − штраф_короткой_истории        # новинка (<60 дней, раздел 5.2) = 25, короче полного
                                   # сезонного цикла = 10, иначе 0 — низкий WAPE на малой
                                   # выборке ничего не гарантирует, штраф отдельный от WAPE
  − штраф_расхождения_моделей     # |победитель_бэктеста − DeepAR| / среднее(двух) × 30,
                                   # максимум 20 пунктов — две независимые модели разошлись
  − штраф_свежего_отклонения      # status=warning в последних периодах (раздел 5.8) = 10,
                                   # critical = 20, свежая запись в model_recalibration_log
                                   # (младше одного цикла пересмотра) = 10, суммируются
  − штраф_stockout_доли           # доля дней is_stockout_corrected (раздел 5.1) за период × 15 —
                                   # высокая доля означает спрос в основном реконструирован

достоверность_% = max(0, достоверность_%)   # не может уйти в минус, 0 — нижняя граница
```
Округляется до целого для отображения. Цветовая подсветка для быстрого сканирования поверх числа — не вместо него: ≥70% зелёная, 40–69% жёлтая, <40% красная — но на странице всегда виден сам процент, не только цвет.

**Пояснение — отдельное поле рядом с процентом, не скрытый расчёт.** Собирается из тех же компонентов, что и сам процент, но человекочитаемым текстом — перечисляются только штрафы, которые реально сработали (нулевые не показываются, чтобы не шуметь):
```
пояснение = список сработавших штрафов через запятую, например:
  "Новинка (-25), Lumpy-паттерн (-20), WAPE 35% (-14)"
  # или, если ничего не сработало:
  "Все факторы в норме"
```
В интерфейсе — раскрывающееся поле рядом с числом (по клику/наведению разворачивается, свёрнуто не мешает читать таблицу). В Excel-выгрузке (раздел 6) — обычный текстовый столбец, раскрывать там нечего, но содержимое то же самое.
Назначение — не заменить решение менеджера, а подсветить, каким рекомендациям можно доверять с меньшей проверкой, а какие (новинка + Lumpy + свежий пересмотр метода одновременно) требуют взгляда человека перед подтверждением, даже если рекомендованное количество выглядит разумным на глаз.

### 5.9 Надёжность поставщика

Упущенные продажи возникают не только от ошибки прогноза, но и от срыва сроков поставщиком. Фиксируем факт: заявленный `lead_time` из справочника поставщика (раздел 3) против фактической даты готовности каждой поставки. Агрегированный `on_time_delivery_rate` используется как модификатор буфера — поставщику с низкой надёжностью закладывается увеличенный lead time в расчёте точки заказа (раздел 5.5), а не номинальный из договора.

### 5.10 Ценовая эластичность и сценарный анализ

**Проблема, которую закрывает раздел:** правило "тестовое повышение цены" (раздел 5.7, маржа > 80%) сейчас работает вслепую — подняли цену, потом постфактум смотрим по воронке, что случилось. С историческими данными о продажах при разных ценах (обычная цена + акции, раздел 4) можно оценить эффект заранее.

**Оценка эластичности:**
```
ln(спрос) = a + b × ln(цена) + контрольные_переменные (сезон, конкуренция по категории)
эластичность = b
```
Считается по `product_model_id`, если хватает исторических точек с разной ценой (акции дают эту вариацию); при недостатке истории — fallback на уровень категории (менее точно, но лучше отсутствия оценки).

**Продвинутый вариант — бустинг с monotone constraints.** Когда данных по цене/спросу достаточно (после накопления истории), log-log регрессия — грубое приближение реальной, часто нелинейной зависимости. Точнее — `XGBoost`/`LightGBM` с `monotone_constraints = -1` по признаку цены: модель ищет сложную нелинейную зависимость, но гарантированно не выдаст "рост цены увеличивает спрос" — ограничение сохраняет экономический смысл результата, которого чистый ML без constraints не гарантирует.

**Сценарный анализ (what-if):** перед принятием ценового решения — прогон гипотезы, а не только мониторинг по факту:
```
прогноз_спроса_при_новой_цене = прогноз_спроса_текущий × (1 + эластичность × %Δцена)
ожидаемое_изменение_маржи = f(новая_цена, прогноз_спроса_при_новой_цене, себестоимость_ед, комиссия)
```
Правило из раздела 5.7 ("маржа > 80% + стабильный спрос → тестовое повышение цены") теперь сначала прогоняется через сценарий — если ожидаемое падение спроса перекрывает выигрыш в марже, рекомендация не выдаётся вообще, а не проверяется по факту после того, как цена уже изменена.

Сценарии, которые применили (а не только просчитали), логируются — чтобы позже сверить прогноз сценария с фактом и калибровать оценку эластичности.

**A/B-подход к тестовому повышению цены (правило из раздела 5.7).** На маркетплейсе нельзя случайно разделить покупателей на группы — все видят одну цену одновременно, значит классический A/B не применим напрямую. Вместо этого — **разница-в-разницах (difference-in-differences)**: сравниваем изменение спроса у тестируемого SKU с изменением спроса у похожих SKU, цену которых не трогали, за тот же период.

```
Тестовая группа:    SKU, где сработало правило "маржа > 80% + стабильный спрос"
Контрольная группа: похожие SKU (из product_analogs — вес_сходства, либо тот же сегмент ABC×XYZ
                     и категория), цена которых в этот период не менялась

эффект = (Δспрос_тестовая − Δспрос_контрольная)
```
Вычитание контрольной группы убирает общий сезонный/трендовый шум периода — эффект не весь приписывается изменению цены, только та часть, которая отличается от естественного движения спроса у похожих товаров.

**Окно теста:** короткое (1-2 недели) — не сразу на весь остаток и не навсегда. По итогам:
- эффект в пределах ожидания (прогноз из сценария, раздел выше) → цена сохраняется, распространяем на весь остаток/похожие размеры
- эффект хуже прогноза за пределами допуска → откат к прежней цене, `price_ab_test.decision = rollback`

Так сценарный анализ (прогноз до) и A/B-тест (факт после) замыкают цикл — сначала оцениваем ожидаемый эффект, потом измеряем реальный на малом окне, и только затем принимаем решение на весь ассортимент.

### 5.11 Оптимизация цен по всей линейке

**Отличие от раздела 5.10:** там — сценарий/тест для одного SKU по отдельности. Здесь — одна задача оптимизации сразу по всей линейке, учитывающая, что цены товаров одной категории не независимы (кросс-эластичность, раздел 5.3): изменение цены одного SKU задевает спрос на связанные.

```
Переменные:  Δp_i — % изменения цены каждого SKU i относительно текущей

Спрос (линейное приближение вокруг текущей цены — корректно при малых Δp,
других и не допускаем из-за guardrail):
  Δспрос_i = эластичность_i × Δp_i + Σ_j (кросс_эластичность_ij × Δp_j)
             # первое слагаемое — собственная эластичность (5.10)
             # второе — влияние цен связанных SKU (cross_elasticity / promo_cannibalization_pairs, 5.3);
             #          для большинства SKU без известных связей это слагаемое = 0

Целевая функция:  максимизировать  Σ_i  маржа_ед_i(Δp_i) × спрос_i × (1 + Δспрос_i)
                  # суммарная маржа по линейке, не по одному SKU
                  # спрос_i = demand_forecast.forecast_qty на период_охвата планируемого изменения

Ограничения:
  |Δp_i| ≤ max_step_pct                     # guardrail на резкость шага (раздел 5.7)
  мин_маржа ≤ маржа_%_i(Δp_i) ≤ макс_маржа  # пороги раздела 5.7, здесь — жёсткое ограничение, не триггер
```

⚠️ **Оптимизация без остатков сама по себе не гарантирует исполнимость.** Формула выше максимизирует маржу по экономике, но не знает, что снижение цены на товар, который вот-вот уйдёт в дефицит (остаток близко к точке заказа, раздел 5.5), бессмысленно — скидка потеряется на нехватке физического товара, не даст обещанный объём продаж. Перед передачей в `pricing_recommendations` результат фильтруется: снижение цены исключается или помечается предупреждением для SKU, где `остаток_FBO + в_пути < прогноз_спроса на период_охвата акции`.

**Решается численно** — `scipy.optimize.minimize` (SLSQP). Для масштаба AWER (сотни SKU) задача лёгкая, промышленный солвер не нужен. Структура разреженная: SKU без кросс-эффектов с соседями по факту оптимизируются независимо, задача распадается на небольшие связанные группы (артикул + его конкуренты по категории), не решается как единый плотный блок.

**Периодичность:** не ежедневно — раз в неделю, либо по триггеру значимого изменения входных данных (курс валюты, раздел 5.7).

**Результат не применяется автоматически** — пакет рекомендаций уходит в уже существующий механизм ручного подтверждения (раздел 6), менеджер видит весь список сразу и подтверждает пакетом или по одному; применение — тем же API-механизмом.

⚠️ Ограничение "отступ от цены конкурента" (упоминалось в разделе про динамическое ценообразование, раздел 11) сюда не включено — по-SKU данных о ценах конкурентов нет, только грубый `category_market_trends.avg_category_price` (раздел 5.6). Задел на будущее при появлении точного источника.

---

## 6. Выходные рекомендации

⚠️ **Приоритет (MVP) — две отдельные страницы вывода, не строки одного общего дашборда.** Отгрузка с ЦС и закупка у поставщика — разные рабочие сценарии для разных решений (первое — просто отгрузить то, что уже есть на складе; второе — заказать то, чего нет), поэтому разводим по разным экранам с самого начала, а не объединяем в одну таблицу с фильтром:

1. **Страница «Рекомендации по поставке с ЦС на FBO»** — только по SKU, где `остаток_1С > 0` (товар уже есть на своём складе, просто нужно решить, сколько и когда отгрузить на Lamoda). Источник — `shipment_recommendations` (ниже). Выгрузка в Excel — отдельный файл/лист.
2. **Страница «Рекомендации по закупке у поставщика»** — SKU, где своего остатка не хватает и нужен заказ у поставщика, по агрегированному спросу со всех каналов сразу (Lamoda+WB+Ozon+собственный сайт+розница, раздел 5). Источник — `purchase_recommendations` (ниже). Выгрузка в Excel — отдельный файл/лист, не общий с первой страницей.

Обе страницы читают из уже описанных ниже таблиц-источников, разделение — только на уровне представления (UI + экспорт), не новая логика расчёта.

**Отгрузка на FBO — точная структура страницы/выгрузки:**
```
артикул | цвет | размер | остаток_на_ЦС | остаток_FBO | рекомендация_к_отгрузке |
степень_достоверности_% (раздел 5.8, число 0-100 с цветовой подсветкой) |
пояснение_достоверности (раскрывающееся поле, текст сработавших факторов) |
подтверждено_к_поставке
```
- `остаток_на_ЦС`, `остаток_FBO` — снимок текущего наличия (см. выше), не расчётные величины
- `рекомендация_к_отгрузке` — то, что считает система (`qty_to_ship`)
- `подтверждено_к_поставке` — **пустое поле по умолчанию**, менеджер вручную вписывает финальную цифру. Не автоматически равно рекомендации — управленческое решение остаётся за человеком, система только предлагает.

**Сборка поставки — по заполненным строкам, отдельным действием.** Поставка собирается не из всей страницы рекомендаций, а только из строк, где менеджер заполнил `подтверждено_к_поставке`. Действие «Собрать поставку» берёт только эти строки и формирует Excel-выгрузку в том же формате и с теми же колонками, что и вся страница — но это уже готовый документ для передачи в пайплайн отгрузки (УПД / Честный Знак / ЭТрН), а не список рекомендаций на рассмотрение. Заполнение поля переводит строку в статус `confirmed` (раздел 10) — так «что подтверждено» отличимо от «что ещё рассматривается» без отдельного флага.

⚠️ "В пути к клиенту" и "в пути от клиента" — **намеренно не колонки этой страницы**. Они участвуют в расчёте `прогноз_остатка(T)` (раздел 5.5) как входные данные формулы, но на самой странице рекомендаций не показываются — страница про решение "сколько отгрузить сейчас", а не про разбор компонентов расчёта.

→ стыкуется с существующим пайплайном УПД / Честный Знак / ЭТрН.

⚠️ **Штраф за позднюю/резкую корректировку уже созданной поставки.** Lamoda штрафует, если корректировка внесена позже допустимого срока и при этом объём отличается от начального плана более чем на 15% (штраф — фиксированная сумма за единицу). Если пересчёт прогноза предлагает изменить объём уже созданной (не отгруженной) поставки более чем на 15% близко к плановой дате — система должна показать это как предупреждение с ожидаемым штрафом, а не просто молча выдать новую рекомендацию поверх старой.

**Закупка у поставщика:**
```
sku_id, размер, кол-во_к_закупке, поставщик_id, срок_готовности
```
Формируется при `точка_заказа > остаток_1С + остаток_FBO + в_пути` и `доступен_к_заказу_у_поставщика = true`.

**Ценообразование:**
```
sku_id, текущая_маржа_%, рекомендация (повысить/снизить/тест/без изменений), обоснование
```

**Дашборд «Прогноз-факт»:**
```
sku_id, размер, категория, сегмент_ABC×XYZ, период,
прогноз, факт, отклонение_%, статус (норма/предупреждение/критично),
причина_отклонения (курс/спрос/комиссия — если применимо, раздел 5.7)
```
Табличное представление построчно по SKU (источник — `forecast_accuracy_log`, раздел 5.8), с фильтрами по категории/сегменту/статусу и сводкой сверху (общий MAPE/WAPE, доля SKU в статусе "критично"). Строки со статусом "предупреждение"/"критично" сортируются наверх — не нужно скроллить весь ассортимент, чтобы найти то, что требует внимания.

**Ручные корректировки прогноза.** Алгоритм не знает того, что знает менеджер — договорённость о крупной оптовой закупке через Lamoda, инфоповод, коллаборацию. Прогноз можно скорректировать вручную поверх расчётного:
```
sku_id, размер, период, прогноз_расчётный, прогноз_скорректированный,
причина (свободный текст), кто_скорректировал, дата
```
Скорректированное значение используется вместо расчётного во всех downstream-расчётах (точка заказа, раздел 5.5) на срок действия корректировки. Расчётное значение никогда не перезаписывается — только добавляется новая запись override поверх, история видна целиком, а не теряется при следующем автопересчёте.

**Применение ценовых рекомендаций через API (ручное подтверждение).** Система только рекомендует (`pricing_recommendations`, раздел 5.7) — цену на Lamoda меняет менеджер, по явному подтверждению, а не автоматически по расписанию.

```
Поток: pricing_recommendations (status=pending) → менеджер видит в дашборде → подтверждает →
       вызов Lamoda API → лог применения (успех/ошибка/на модерации)
```

**Метод API (FBO, v2):** `POST /v2/nomenclature-price` — параметры `lamoda_parent_sku` (не `seller_sku` — тот для FBS), `partner_id`, `country`, `price`/`salePrice` (объекты `{amount, currency}`, amount — целое число в копейках), `saleStart`/`saleEnd` (camelCase, полный ISO-datetime). Для массового подтверждения нескольких SKU разом — `POST /v2/nomenclatures-prices`, а не по одному вызову на SKU. (v1-эквиваленты `v1.nomenclature.update-price`/`v1.nomenclatures.set-prices` — не использовать в новой разработке, отключаются 01.01.27, раздел 7).

⚠️ **Частая ошибка "Партнёр не найден" (50400)** — если перепутать метод под модель: FBS-метод — идентификатор `seller_sku`, FBO-метод (наш случай) — идентификатор `lamoda_parent_sku` + `partner_id`. Использование не того метода для нашей модели (FBO) даёт эту ошибку, а не тихо работает с другим набором параметров.

⚠️ **Ценовой карантин Lamoda:** снижение цены больше чем на 70% не применяется мгновенно даже через API — уходит на модерацию в ЛК (раздел "Цены" → "На модерации"). Система должна предупреждать об этом менеджера **до** подтверждения ("это снижение уйдёт на модерацию, применится не сразу"), а не молча ждать несуществующего немедленного эффекта. Результат модерации приходит вебхуком (`moderationApproved`/`moderationRejected`, раздел 4) для быстрой реакции — статус `quarantined` в `price_change_log` обновляется по событию, но регулярный опрос за период остаётся страховкой на случай недоставленного вебхука (раздел 4 — вебхуки не гарантируют доставку).

⚠️ **Валидация акционной цены по медиане 30 дней и фрод-проверка.** Lamoda рассчитывает медианную цену за последние 30 дней и отклоняет `sale_price`, если скидка не проходит их порог валидации — независимо от нашего собственного guardrail `max_step_pct` (раздел 5.11). Отдельно — автоматическая фрод-проверка цены (`fraudValidationResult` в ответе метода), ещё один самостоятельный фильтр. Обе проверки внешние, не наши — `api_response_status` в `price_change_log` различает `validation_failed` от общего `api_failed` (раздел 10).

⚠️ **HTTP 200 не гарантирует применение цены.** Успешный код ответа не означает, что цена принята — нужно проверять поля `errors[]`, `priceStatuses`, `fraudValidationResult` в теле ответа, а не только HTTP-статус. Модуль применения цен (раздел 6) парсит тело ответа на каждый вызов, не считает успехом факт отсутствия HTTP-ошибки.

**Два разных механизма скидки — оба доступны по API v2, не путать.** (1) Участие в акциях Lamoda (с возможной доплатой от них) — `POST /v2/promotion-variants/{promotionVariantId}/products`/`remove-products` (раздел 4) — добавление/удаление товаров из уже существующей акции Lamoda, не создание своей акции. (2) Собственная временная скидка за счёт продавца, без участия в программе Lamoda — `salePrice`/`saleStart`/`saleEnd` в том же методе `POST /v2/nomenclature-price`, что и обычная цена (раздел 6 выше). Для A/B-теста (раздел 5.10) и применения результата оптимизации по линейке (раздел 5.11) используем именно второй механизм — не пытаемся встроить тест в чужую акцию Lamoda с её собственными сроками и условиями участия.

**Статусы `pricing_recommendations`:** `pending` (ждёт решения) → `approved` (менеджер подтвердил) → `applied` (API вернул успех) / `api_failed` (ошибка вызова) / `quarantined` (ушло на модерацию Lamoda, ждёт их подтверждения) — расширяет уже существующие `active`/`suppressed`/`superseded` (раздел 10).

---

## 7. Технологический стек

Переиспользуем существующую инфраструктуру:
- FTP-мост 1С ↔ Python (уже отработан в проекте «Синхронизация Остатков»): обмен через `.epf` внешнюю обработку на стороне 1С (читает задания из FTP, создаёт документы движения — без изменений в конфигураторе), тот же принцип и для потока остатков в эту систему
- Webhook-эндпоинт (FastAPI POST-хендлер, HTTPS, URL ≤1024 символов, доступен 24/7, отвечает <5 секунд, опционально Basic Auth) — приём нотификаций Lamoda (раздел 4): `statusChanged`/`itemStatusChanged` (заказы), `moderationApproved`/`moderationRejected` (модерация цены), `fulfilmentShipmentValidationRest` (валидация поставки FBO). Ответ `2xx` = доставлено; `4xx` = без повтора (проблема на нашей стороне); `5xx`/timeout = Lamoda повторяет с растущим интервалом, при исчерпании — недоставленные нотификации по заказам можно запросить повторно отдельным механизмом переотправки.
- Разработка и тестирование на `api-demo-b2b.lamoda.ru` (тестовые данные, отдельные credentials) — переключение на прод (`api-b2b.lamoda.ru`) только после стабилизации, не сразу на живых заказах
- REST B2B Platform не поддерживает пакетную обработку — только последовательные запросы (в отличие от JSON-RPC Lamoda Seller, где batch есть — напр. `set-prices`) — модуль забора данных закладывает это в темп опроса заказов/отгрузок
- Python/FastAPI backend
- **`statsforecast` (Nixtla)** — готовая открытая библиотека для прогноза спроса, включая `CrostonSBA`, `TSB`, `IMAPA`, `ADIDA` для прерывистого спроса (раздел 5.2) — не пишем эти модели с нуля
- **`GluonTS` (awslabs, Apache 2.0, бесплатно)** — реализация DeepAR (раздел 5.2), обучается на CPU за реалистичное время (порядка часа) при масштабе AWER, GPU не требуется
- **`wbsdk`** (Python) — готовый клиент Statistics API Wildberries (раздел 4) — синхронный/асинхронный, покрывает нужные разделы (Reports/Analytics) для исторической выгрузки продаж
- **`pavelmaksimov/tapi-yandex-direct`** и **`pavelmaksimov/tapi-yandex-metrika`** — готовые Python-клиенты для API Директа (включая Wordstat-отчёты — тот же API, не отдельный сервис) и всех API Метрики (Stats/Logs/Management, раздел 4) — не пишем HTTP-обёртки с нуля
- SQLite/Postgres для хранения расчётных сущностей
- Существующий пайплайн УПД-конструктор / Честный Знак / ЭТрН как приёмник выходных рекомендаций по отгрузке

⚠️ **Строить на Lamoda Seller Partner API v2 (REST) обязательно, не по предпочтению — оба API v1 (JSON-RPC и B2B Platform REST) отключаются 01.01.27.** От текущей даты разработки это меньше 4 месяцев — использование v1-методов в новой системе создаёт риск, что интеграция перестанет работать раньше, чем система выйдет из активной разработки. Базовый URL v2: `https://public-api-seller.lamoda.ru/api`. Ниже — таблица соответствия методов, использованных в этом ТЗ, их v2-эквивалентам (обнаружено полным списком из 74 эндпоинтов v2 при изучении документации):

| Раздел ТЗ | Было (v1 JSON-RPC) | v2 REST |
|---|---|---|
| 6 (применение цены) | `v1.nomenclature.update-price` | `POST /v2/nomenclature-price` |
| 6 (массовое применение) | `v1.nomenclatures.set-prices` | `POST /v2/nomenclatures-prices` |
| 3 (сопоставление SKU) | `v1.nomenclatures.list` | `GET /v2/nomenclatures` (обязателен параметр `country`) |
| 8.1 (атрибуты товара) | `v1.attributes.sku.list` | `GET /v2/nomenclatures/{sku}/attributes` |
| 8.1 (атрибуты категории) | `v1.attributes.list` | `GET /v2/nomenclature-categories/{categoryId}/attributes` |

**Правила обратной совместимости v2** — код должен быть защищён от несовместимых изменений заранее: новые поля/методы/enum-значения добавляются Lamoda без анонса (в любой момент), поэтому JSON-парсер игнорирует незнакомые поля, а обработчик статусов (`items[].status` и т.п., раздел 4) не должен падать или зависать на неизвестном значении статуса — логировать и пропускать, не прерывать пайплайн. Ломающие изменения анонсируются заранее (2-4 недели на смену поведения метода, 4+ недель на новый обязательный параметр, 3+ месяца на депрекацию метода) — канал `@apilamoda` (раздел 11) — единственный оперативный источник этих анонсов.

**Референс по структуре API Lamoda при реализации** — официальные репозитории `lamoda/seller-center-sdk-php` и `lamoda/lamoda-b2b-platform.php-sdk` на GitHub (компания в составе Global Fashion Group). Хоть и на PHP, это авторитетный источник точной структуры эндпоинтов B2B Platform REST API — сверяться с ним наравне с документацией на этапе написания модуля забора данных.

### 7.1 Требования к серверу

Оценка при среднем потоке 50-100 заказов/день на Lamoda (~27 тыс./год) и каталоге порядка 1000-3000 активных `product_size`. Прикидка по архитектуре, не результат нагрузочного теста — перепроверить на реальных цифрах после первых недель работы.

⚠️ **Многоканальность (раздел 5) увеличивает вычислительную нагрузку кратно, не на проценты.** Бэктест-турнир и DeepAR теперь прогоняются отдельно по каждой паре (`product_size`, `channel`) — Lamoda/WB/Ozon/собственный сайт, то есть до ~4× объёма против исходной оценки ниже, которая считалась только под Lamoda. Данных для хранения по-прежнему немного (см. ниже), но еженедельное батч-окно стоит закладывать с запасом, не буквально "час", а несколько часов — и перепроверить на реальных цифрах после первых недель многоканальной работы отдельно, не только для Lamoda.

**Хранение.** Большинство таблиц — временные ряды, растущие построчно, но записи мелкие. Самая тяжёлая — `webhook_events_log` (раздел 4, JSONB payload на каждое событие) — до нескольких сотен МБ в год. Остальное (заказы, прогнозы, маржа, эластичность — теперь по нескольким каналам сразу, но объём каждой записи не меняется) — суммарно низкие сотни МБ в год, может быть до 2-3× больше с учётом WB/Ozon/сайта. Даже за 5 лет работы — единицы ГБ, узкое место точно не здесь.

**Вычисления — пиковые батч-окна, не постоянная нагрузка.** Croston/TSB/сглаживание — секунды на SKU-канал. DeepAR/GluonTS (раздел 5.2) — самый тяжёлый шаг: изначально оценивался как около часа на CPU для одного канала (Lamoda) — с учётом четырёх каналов реалистичнее закладывать несколько часов на полный еженедельный цикл, всё ещё ночью и всё ещё на CPU, просто с более широким окном. Оптимизация цен по линейке (раздел 5.11, пока только Lamoda) — секунды. Днём, вне батч-окна, нагрузка минимальна — приём вебхуков (доли секунды на событие), обычные API-запросы по расписанию.

**AI-агенты (раздел 8)** — вызовы Claude API, не локальные вычисления. Не требуют ни GPU, ни существенного CPU/RAM на своей стороне — только сетевой запрос.

**Рекомендация — один сервер, ничего экзотического:**

| Параметр | Значение | Почему |
|---|---|---|
| CPU | 4-8 vCPU | хватает и на дневную нагрузку, и на еженедельный DeepAR-батч за разумное время |
| RAM | 8-16 ГБ | Postgres + Python-процессы + датафреймы бэктеста по тысячам рядов одновременно |
| Диск | 50-100 ГБ SSD | база растёт медленно, запас — под логи, Excel-выгрузки, артефакты моделей |
| GPU | не нужен | подтверждено разделом 5.2 — DeepAR на этом масштабе реалистичен на CPU |
| Доступность | 24/7, стабильный IP/домен с TLS | обязательно для вебхук-эндпоинта (раздел 7 — HTTPS, <5 сек ответ) |

Обычный VPS среднего тарифа (4-8 vCPU / 8-16 ГБ) закрывает всё — от вебхуков до еженедельного переобучения, даже с учётом кратного роста батч-нагрузки от многоканальности выше (батч-окно просто становится шире, не требует больше ядер/памяти одновременно, если DeepAR по каналам считается последовательно, не параллельно). Дальнейшая оптимизация возможна (маленький постоянный инстанс под API/вебхуки/БД + отдельный, включаемый по расписанию под тяжёлый батч-пересчёт), но при таком масштабе это экономия ради экономии, не необходимость.

---

## 8. AI-агенты

Агенты используются только там, где нужна интерпретация неструктурированных данных или суждение в пограничных случаях. Основной расчётный контур (прогноз спроса, точка заказа, маржа, ABC-классификация) остаётся чистой арифметикой — без LLM, ради воспроизводимости и объяснимости.

### 8.1 Vision-агент: подбор тегов фасона

**Назначение:** повышает точность подбора аналогов для новинок (раздел 5.6) — вместо сходства только по категории/цене/сезону добавляет сходство по силуэту/фасону.

**Перед запуском — проверка `v1.attributes.sku.list`.** У категории может уже быть нужный атрибут (фасон/посадка/силуэт), заполненный вами при создании карточки — тогда он надёжнее и дешевле, чем вывод по фото. Vision-агент запускается только если у категории нет подходящего структурированного атрибута.

```
Вход:  фото карточки SKU (URL из карточки Lamoda / собственного каталога)
Модель: Claude vision
Выход: sku_id, теги_фасона[] (из фиксированной таксономии — оверсайз, приталенный,
       принт, длина, посадка и т.п.), confidence
```

**Триггер:** при заведении новой карточки/новинки — разово, не периодическая задача.

**Human-in-the-loop:** если `confidence` низкий, или агент предлагает тег вне существующей таксономии — карточка помечается на ручную проверку, а не пишется в справочник автоматически.

**Куда пишет:** поле `теги_фасона` в справочнике товара (раздел 3) → используется в расчёте `вес_сходства` в справочнике аналогов.

### 8.2 Агент триажа аномалий воронки

**Назначение:** когда по SKU одновременно срабатывает несколько сигналов (падение конверсии в корзину + рост возврата + скачок курса и т.п.) — вместо того, чтобы менеджер вручную реконструировал причину по таблицам, агент формулирует вероятный диагноз.

```
Вход:  набор сработавших сигналов по SKU за период
       (доля отменённых/недоставленных заказов, возвратность, курс USD, флаг неликвида, изменение маржи)
Модель: LLM (reasoning), структурированный вывод
Выход: sku_id, вероятная_причина, рекомендуемое_действие, уверенность
```

**Триггер:** батч-джоб (ежедневно/еженедельно), запускается только для SKU, где сработало ≥ 2 сигнала одновременно — единичные сигналы и так покрыты детерминированными правилами из раздела 5.3, агент не дублирует их.

**Human-in-the-loop:** менеджер подтверждает или отклоняет рекомендацию агента. Решения (подтверждено/отклонено) логируются — это будущая обучающая выборка для калибровки правил, а не для дообучения модели.

**Связь с ценовыми рекомендациями (решено):** если менеджер подтверждает диагноз, объясняющий отклонение маржи неактуальной причиной (например, «маржа упала из-за курса, это нормально») — связанная автоматическая рекомендация по цене (раздел 5.7) для этого SKU и периода подавляется, а не выполняется автоматически. Статус рекомендации меняется на "suppressed" со ссылкой на запись триажа, которая это вызвала.

---

## 9. Открытые вопросы (требуют уточнения перед/в процессе разработки)

0. **Приоритетно, до начала реализации:** свериться со справочником соответствия методов v1→v2 Lamoda и переписать все обращения к API в этом ТЗ на v2 — поддержка v1 заканчивается 01.01.27 (раздел 2/7)
1. Точная схема ответа `GET /v2/promotions/{promotionId}/products` — есть ли разбивка «цена за счёт продавца» отдельно от общей скидки (раздел 4/5.7). API найден, но полная схема не подтверждена — проверить на этапе реализации (документация или тестовый запрос)
2. У `api-integration@lamoda.ru`: формула компенсации скидки на комиссию для ставки НДС 5%
3. Источник данных по погоде (конкретный API — обсудить при реализации)
4. Точные статусы заказов, которые считать "продажей" против "возврата/отмены" — свериться со Swagger-спецификацией
5. Точный метод Ozon Seller API для получения комиссии по категориям (раздел 4/5.7-доп) — в изученной документации подтверждён только общий раздел FinanceAPI/тарифы, конкретный эндпоинт не проверен; на старте можно брать комиссию из отчёта о продажах, если она указана построчно
6. API Яндекс KIT для заказов (раздел 4) — уже прорабатывается отдельно в Claude Code (там установлены нужные скиллы, недоступны из этого документа/чата) — при реализации модуля забора данных для собственного сайта сверяться с тем, что там наработано, а не с предположением "только CSV/XLSX" из раздела 4 выше

---

## 10. Структура БД

Полная схема — отдельный файл `lamoda_forecast_schema.sql`. Ключевые решения:
- Иерархия `product_models` (артикул+цвет) → `product_sizes` (+размер); прогноз/остатки/маржа — на уровне `product_sizes`, аналоги — на уровне `product_models`. Таблицы `funnel_model`/`funnel_size` в схеме остаются, но не заполняются автоматически (нет API) — опциональны при ручном разборе, если понадобится точечная проверка
- `fbo_shipments` — факт созданных поставок (источник компонента «в пути на FBO» в разделе 5.4, отдельно от `shipment_recommendations`, которые лишь рекомендация)
- `stock_reconciliation_log` — сверка расчётного состояния (state-machine) с фактическим снимком `stock_fbo`
- `new_item_size_allocation` — явный шаг раскладки агрегатного прогноза новинки (уровень модели) по размерам через `size_profile`
- `cannibalization_events` — хранит сигнал каннибализации между цветами/размерами, используемый в прогнозе (раздел 5.3)
- `size_profile` включает поле `gender` (профиль отличается по полу — раздел 5.6)
- `margin_settings.commission_vat_multiplier` — заглушка под множитель компенсации комиссии по ставке НДС 5% (по умолчанию 1, обновить после ответа Lamoda, открытый вопрос №2)
- `abc_xyz_classification`, `forecast_accuracy_log`, `supplier_deliveries` — классификация по индустриальной практике (ABC×XYZ, MAPE/WAPE, надёжность поставщика), раздел 5.8-5.9
- `demand_forecast.demand_pattern` — SBC-классификация (Smooth/Irregular/Intermittent/Lumpy по ADI×CV²), заменила бинарный флаг прерывистости, раздел 5.2; `model_backtest_results` — турнир методов, включая DeepAR (`GluonTS`) как отдельного, обучаемого один раз на весь каталог кандидата
- `cross_elasticity` (внутри артикула, по доступности) и `promo_cannibalization_pairs` (между разными артикулами, по акции, t-test) — два отдельных механизма каннибализации, не путать, раздел 5.3
- `price_optimization_run`/`price_optimization_result` — оптимизация цен сразу по всей линейке (не по одному SKU), раздел 5.11; результат фильтруется по остаткам (`stock_check_passed`) перед выдачей рекомендации
- `webhook_events_log` — идемпотентная обработка вебхуков Lamoda по ключу «id заказа + статус», вебхуки — ускоритель, не замена регулярного опроса (раздел 4)
- `price_change_log` различает три независимые внешние проверки при применении цены: карантин (>70% снижения), валидация по медиане 30 дней, фрод-проверка (раздел 6)
- Суммы из Lamoda API — минорные единицы (копейки), конвертация на границе приёма данных (раздел 7); методы API в тексте раздела 3-8 частично оставлены как v1 с явной пометкой — таблица соответствия v1→v2 в разделе 7
- `demand_forecast`/`margin_calc` — многоканальные (поле `channel`: lamoda/wb/ozon/own_site, раздел 5/5.7-доп); тот же аппарат прогноза (SBC/TSB/DeepAR/бэктест) прогоняется отдельно по каждой паре SKU+канал
- `central_stock_aggregate_demand` — суммарный спрос по всем каналам сразу, источник для точки заказа/закупки у поставщика (раздел 5.5/6), т.к. WB/Ozon/собственный сайт (не-Lamoda каналы) тянут остаток напрямую с ЦС, без промежуточного склада площадки
- `external_channel_sales_history`/`channel_ratio_calibration`/`seasonal_index_reference` — историческая выгрузка WB/Ozon/розницы (5-7 лет, разовая загрузка) для сезонного индекса и прогноза новинок с историей на другом канале (раздел 4/5.2/5.6)
- `underperformance_flags` — детектор «недооценённого потенциала» (сильные продажи на другом канале, почти нулевые на Lamoda) с пятью автоматическими диагностическими проверками, раздел 5.6
- `recommendation_confidence` — степень достоверности рекомендации в процентах с пояснением сработавших факторов, раздел 5.8/6
- `channel_commission_rates` — комиссия/логистика по каналу и категории (WB: Tariffs API подтверждён; Ozon: метод не подтверждён, открытый вопрос №5, раздел 5.7-доп)
- `barcode_pool` — сопоставление по совпадению любого одного баркода из пула, не единственного канонического значения (раздел 3)

## 11. Академические и открытые источники, использованные в проектировании

**Методы (академическая литература):**
- **Newsvendor model** (Arrow et al. 1951, восходит к Edgeworth 1888) — критическая доля для объёма первой поставки новинок, раздел 5.6
- **Croston (1972) / Syntetos-Boylan Approximation (2001)** — прогноз прерывистого спроса, раздел 5.2
- **Johnston & Boylan (1996), Syntetos-Boylan-Croston (2005)** — классификация типа спроса по ADI×CV² (Smooth/Irregular/Intermittent/Lumpy), раздел 5.2
- **Nahmias (1994), Wecker (1978)** — censored demand estimation, академическое обоснование коррекции спроса на stockout, раздел 5.1; риск spiral-down эффекта и Tobit Exponential Smoothing как путь апгрейда
- **Cycle service level (Type 1) vs fill rate (Type 2)** — стандартное разделение в теории управления запасами, применено в разделах 5.2/5.5 (буфер) и 5.8 (мониторинг)

**Отраслевой разбор (проверка формул на практике):**
- **Forecast NOW!** (fnow.ru) — детальный разбор формулы страхового запаса `k×σ×√L`: незащищённое окно должно включать интервал пересмотра, σ считается по ошибке прогноза (не по сырым продажам), расширенная формула при нестабильном lead time, фильтр выбросов 3σ — все учтено в разделе 5.5. Также подтверждение, что классические методы (SMA, экспоненциальное сглаживание, Хольта-Винтерса) дают одно число без распределения, что ограничивает их применимость для около 94% ассортимента с негладким спросом
- **Beltel Datanomics** — эмпирическое сравнение Croston/TSB против Naive/SES на реальных данных оптово-розничной сети: TSB даёт лучшие результаты по большинству метрик, подтверждён выбор TSB как предпочтительного метода для Lumpy-категории (раздел 5.2); также источник метрик дефицита/профицита (раздел 5.8) и метода двухвыборочного t-теста для обнаружения промо-каннибализации между разными артикулами (раздел 5.3)

**Открытый код и референсы (для этапа реализации):**
- **`Nixtla/statsforecast`** (Apache 2.0) — готовые модели `CrostonSBA`, `TSB`, `IMAPA`, `ADIDA` для прерывистого спроса, раздел 5.2/7
- **`awslabs/gluonts`** (Apache 2.0, бесплатно) — реализация DeepAR (Salinas et al., 2017/2019, глобальная вероятностная модель поверх RNN, JMLR) — шестой кандидат бэктест-турнира и альтернативный источник для новинок, раздел 5.2/5.6/7; на масштабе AWER обучается на CPU, GPU не обязателен
- **`wbsdk`** (Python) — готовый клиент Statistics API Wildberries, раздел 4/7
- **`pavelmaksimov/tapi-yandex-direct`, `pavelmaksimov/tapi-yandex-metrika`** (MIT) — готовые Python-клиенты для API Директа/Wordstat и Метрики, раздел 4/7
- **`lamoda/seller-center-sdk-php`, `lamoda/lamoda-b2b-platform.php-sdk`** — официальные репозитории Lamoda (Global Fashion Group), авторитетный референс по структуре эндпоинтов B2B Platform REST API
- **`eslazarev/wildberries-sdk`** и открытые MCP-серверы для WB/Ozon Seller API — структурное подтверждение, что подход из раздела 4 (остатки, воронка продаж как отдельные отчётные эндпоинты) — общая практика API маркетплейсов, не частный случай Lamoda
- **`samirsaci/inventory-periodic`** — референс реализации periodic review policy на Python, релевантно логике частоты пересмотра по сегменту ABC×XYZ (раздел 5.2)

---

## 12. Порядок реализации (для перехода в Claude Code)

Документ большой (полный контекст проекта, не пошаговый план) — чтобы не пытаться поднять всё разом, реализация двигается фазами. Каждая фаза даёт рабочий результат сама по себе, не ждёт следующую:

**Фаза 1 — MVP без ML.** Схема БД целиком (можно развернуть сразу — таблицы не создают циклических зависимостей по фазам). 1С (остатки ЦС) + Lamoda API v2 (остатки FBO, продажи) — ежедневный опрос, без вебхуков пока. Простая точка заказа (раздел 5.5, без SBC/бэктеста — плоское сглаживание как первый прогноз). Страницы 1 и 2 раздела 6 (поставка на FBO / закупка у поставщика) с Excel-выгрузкой — это то, что реально нужно в работе с первого дня.

**Фаза 2 — Прогноз становится умнее, только Lamoda.** SBC-классификация + бэктест-турнир (раздел 5.2, без DeepAR — TSB/CrostonSBA/сглаживание/бустинг достаточно на старте). Цензурирование по stockout (раздел 5.1). Вебхуки вместо чистого опроса (раздел 4/7) — ускоряют реакцию. Маржинальность Lamoda (раздел 5.7) со всеми тремя проверками цены (карантин/медиана/фрод, раздел 6).

**Фаза 3 — Многоканальность.** WB/Ozon постоянные потоки (раздел 4) + `channel` на `demand_forecast`/`margin_calc` + `central_stock_aggregate_demand` (раздел 5) — страница 2 раздела 6 начинает учитывать агрегированный спрос, не только Lamoda. Историческая выгрузка WB/Ozon/розница (раздел 4) — сезонный индекс + метод для новинок с историей на другом канале (раздел 5.6). Собственный сайт — по факту доступности API/выгрузки (раздел 9, вопрос №6).

**Фаза 4 — Продвинутое.** DeepAR/GluonTS как шестой кандидат бэктеста (раздел 5.2) — тяжелее в реализации и по ресурсам (раздел 7.1), ценно, но не блокирует остальное. Ценовая эластичность/сценарии/A-B-тест (раздел 5.10) и оптимизация по линейке (раздел 5.11). AI-агенты (раздел 8) — vision-теги и триаж аномалий. Детектор недооценённого потенциала (раздел 5.6) и степень достоверности (раздел 5.8/6) — оба зависят от накопленной истории по нескольким фазам сразу, раньше смысла не имеют.

Открытые вопросы (раздел 9) не блокируют фазу целиком — большинство имеют заложенный fallback (параметр по умолчанию, ручная сверка) и решаются по ходу, не до старта разработки.


