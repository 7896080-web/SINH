# Статус готовности (актуально, 2026-09-16)

Скорректированный «честный» список — прежний устарел (п.1 и п.3 фактически выполнены).

## 1. HTTP-клиенты площадок на живых ключах — ✅/⚠️ (было: «не проверены»)
- **WB — ✅ проверено на живых ключах.** Реально тянет заказы (520 / 244 / 55 по трём кабинетам),
  историческая выборка `get_orders_since` вернула корректные заказы с датами, `push_stock`
  отрабатывал, бэкфилл создал реальные документы.
- **Kit — ✅ с оговоркой.** Тянет (154 заказа), но ловит `429` (rate limit) → нужен троттлинг.
- **Ozon — ⚠️ не подтверждено.** Эндпоинт отвечает, но вернул 0 заказов → разбор ответа на
  непустых данных не проверен. Нужен кабинет Ozon с FBS-заказами.
- Старый комментарий-предупреждение в `base.py` устарел для WB/Kit.

## 2. Детект «заказ подтверждён» (Впути → Склад) — не требуется при Варианте А
Под текущей моделью (Вариант А) резерв сразу в основном складе площадки
(`warehouse_pending == warehouse_sold`), отдельное перемещение «Ожидает → Склад» намеренно не
создаётся (`process_confirmation` его пропускает). Пункт **не блокирует прод**. Заполнять
`get_confirmed_orders` для WB/Ozon (у Kit уже есть) понадобится только при переходе на
двухшаговую схему.

## 3. Backfill «задним числом» — ✅ реализовано (было: «не реализовано»)
- Через «Дату начала» на /testing → `get_orders_since` тянет реальные FBS-заказы с даты →
  каждый проводится **своей реальной датой** (`FtpTask.movement_date` → 8-е поле → `.epf`).
- `broadcast_active_since` наш бэкфилл **не использует** (оператор задаёт дату вручную). Поле
  осталось неиспользуемым — связать или убрать как устаревшее.
- Проверено: проведено 8 по LACIVERT/RED, фильтр по баркоду точный.

## 4. Автозапуск .epf по расписанию — способ согласован и настроен
Пакетный `1cv8 ENTERPRISE … /CEXCHANGE`, `ПриОткрытии` привязан в палитре, задача в Планировщике
Windows; нужна интерактивная сессия. Осталось — подтвердить стабильность срабатывания.

---

## Что реально открыто сейчас (обновлено 16.09.2026, вечер)

### Закрыто этой сессией
- **(A) Бэкдейтинг end-to-end в 1С — ✅ подтверждён.** Отчёт «Движения товара» по тестовому
  SKU: 9 перемещений реальными датами 11.08…14.09 (ЦБ000000186…194), начальный остаток 43,
  расход 14 (9 заказов + 5 на сторонний склад), конечный 29. Правка `ДатаДок` в `.epf` жива;
  её текст снят из конфигуратора и положен в `1c/` (см. ПЕРЕДАЧА_В_ВЕБ.md §11).
- **(B) Гейт-отбор в воркере — ✅ работает.** Живой опрос обрабатывает только отмеченные пары
  товар+кабинет; выключенные тихо пропускаются.
- **(D) Kit 429 — ✅ троттлинг 0.25 с в клиенте.**
- **Порог трансляции** развёрнут (pm12) и перенесён на единую страницу «Товары и остатки» (pm17).
- **Баг канала 1С:** файлы от обработки читались без учёта BOM — первая строка каждого
  `result_*.txt` не сопоставлялась, задание висело «без результата» при созданном документе
  (pm15). Опоздавший ответ теперь закрывает и задание в статусе `timeout` (pm16).
- **Расчёт отправки** сведён в один модуль `app/transmit.py`: интерфейс и рассылка больше не
  могут разойтись (до этого страница показывала 18, а на площадки уходили нули).

### Открыто
- **(C) Ozon** — разбор заказов на непустых данных не проверен (в выборке было 0 заказов).
- **Трансляция тестового SKU выключена** (`broadcast_enabled = 0`, как и у всех 152 235 товаров —
  это дефолт миграции). Пока не включить кнопкой «Вкл» на «Товары и остатки», на площадки
  уходит 0 независимо от порога.
- **Порог кабинета 32** на кабинетах 1 и 5 у тестового товара: сейчас перекрыт порогом
  трансляции, но при его сбросе обнулит отправку. Стоит вернуть 0.
- **8 конфликтов сопоставления** — предположительно «чужие» баркоды (товара нет в 1С), проверить.
- **Kit: авто-реверс отмен** отключён — вернуть, если Kit начнёт отдавать инициатора отмены.
- **Ротация пароля 1С** — вручную, в чат не вставлять.
- **55 посторонних файлов** в корне `C:\sync_admin` от прошлых сессий деплоя (`diag_*.py`,
  `pm*.py`, `p*.txt`, дампы base64). Предложение: `diag_*.py` и `watch.py` — в `scripts/` и в
  репозиторий, разовые — удалить.
- **`broadcast_active_since`** — связать или убрать как устаревшее (у тестового товара стоит
  2026-07-07, похоже на опечатку вместо 07.08).
